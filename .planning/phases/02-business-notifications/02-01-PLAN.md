---
phase: 02-business-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements/base.txt
  - backend/apps/business/models.py
  - backend/apps/business/serializers.py
  - backend/apps/business/permissions.py
  - backend/apps/business/views.py
  - backend/apps/business/tasks.py
  - backend/apps/business/urls.py
  - backend/apps/business/migrations/0001_initial.py
  - backend/apps/stores/models.py
  - backend/apps/stores/migrations/0002_store_business_profile.py
  - backend/apps/prices/models.py
  - backend/apps/prices/serializers.py
  - backend/config/settings/base.py
  - backend/apps/core/exceptions.py
  - backend/tests/unit/test_business_models.py
  - backend/tests/integration/test_business_registration.py
  - backend/tests/integration/test_business_verification.py
  - backend/tests/integration/test_business_prices.py
  - backend/tests/integration/test_promotions.py
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03

must_haves:
  truths:
    - "A business user can register a BusinessProfile with CIF/NIF and name; the profile starts unverified"
    - "An admin can approve or reject a BusinessProfile via POST /api/v1/business/profiles/{id}/approve/ or /reject/ with a reason string"
    - "Email is sent to the business on approval and on rejection"
    - "An unverified business receives HTTP 403 on write operations (price updates, promotions)"
    - "A verified business can POST a price with source='business' that never goes stale"
    - "A verified business can create, deactivate, and edit promotions; only one active promotion per product+store is enforced"
    - "Active promotions appear as promo_price in price comparison responses"
    - "deactivate_expired_promotions Celery task marks promotions with end_date < today as is_active=False"
  artifacts:
    - path: "backend/apps/business/models.py"
      provides: "BusinessProfile, Promotion models"
      contains: "class BusinessProfile"
    - path: "backend/apps/business/permissions.py"
      provides: "IsVerifiedBusiness permission class"
      contains: "class IsVerifiedBusiness"
    - path: "backend/apps/business/views.py"
      provides: "BusinessProfileViewSet, PromotionViewSet, BusinessPriceViewSet"
      contains: "approve"
    - path: "backend/apps/business/tasks.py"
      provides: "deactivate_expired_promotions, check_competitor_prices, send_business_approval_email, send_business_rejection_email tasks"
      contains: "deactivate_expired_promotions"
    - path: "backend/apps/stores/models.py"
      provides: "Store.business_profile FK (nullable)"
      contains: "business_profile"
    - path: "backend/apps/prices/models.py"
      provides: "Price.Source.BUSINESS choice added"
      contains: "BUSINESS"
    - path: "backend/apps/prices/serializers.py"
      provides: "promo_price field in price comparison"
      contains: "promo_price"
  key_links:
    - from: "backend/apps/business/views.py BusinessProfileViewSet.approve"
      to: "backend/apps/business/tasks.send_business_approval_email"
      via: "task.delay(profile.id)"
      pattern: "send_business_approval_email\\.delay"
    - from: "backend/apps/business/views.py BusinessPriceViewSet.perform_create"
      to: "backend/apps/prices/models.Price source='business'"
      via: "serializer.save(source=Price.Source.BUSINESS)"
      pattern: "Price\\.Source\\.BUSINESS"
    - from: "backend/apps/prices/serializers.py PriceComparisonSerializer"
      to: "backend/apps/business/models.Promotion"
      via: "context['promo_lookup'] dict prefetched in view"
      pattern: "promo_lookup"
---

<objective>
Implement the full business portal backend: BusinessProfile model with admin verification flow, business price updates (source='business', no stale expiry), Promotion model with auto-deactivation, and surface active promotions in the existing price comparison response.

Purpose: Enables PYME businesses to self-register, get verified, manage their store prices manually, and run time-limited promotions — satisfying BIZ-01, BIZ-02, and BIZ-03.
Output: 5 new files in apps/business/, 2 DB migrations, updated prices and stores models, updated price comparison serializer, and full test suite scaffolds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-notifications/02-CONTEXT.md
@.planning/phases/02-business-notifications/02-RESEARCH.md
@.planning/phases/02-business-notifications/02-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the executor must honour. Extracted from Phase 1 codebase. -->

From backend/apps/core/responses.py:
```python
def success_response(data: object, status: int = 200) -> Response: ...
def created_response(data: object) -> Response: ...   # returns 201
```

From backend/apps/core/exceptions.py (existing BargainAPIException subclasses as pattern):
```python
class BargainAPIException(APIException):
    status_code = 400
    default_code = "bargain_error"
    def __init__(self, detail=None, code=None): ...
# New subclasses needed: BusinessNotVerifiedError (403), PromotionConflictError (409)
```

From backend/apps/prices/models.py:
```python
class Price(models.Model):
    class Source(models.TextChoices):
        SCRAPING = "scraping", "Scraping"
        CROWDSOURCING = "crowdsourcing", "Crowdsourcing"
        API = "api", "API oficial"
        # ADD: BUSINESS = "business", "Portal PYME"
    product: FK → products.Product
    store: FK → stores.Store
    price: Decimal
    source: CharField(choices=Source.choices)
    is_stale: BooleanField(default=False)
    confidence_weight: FloatField(default=1.0)
```

From backend/apps/stores/models.py:
```python
class Store(models.Model):
    is_local_business: BooleanField  # set True for business-created stores
    # ADD nullable FK: business_profile → business.BusinessProfile
```

From backend/apps/users/models.py:
```python
class User(AbstractUser):
    class Role(TextChoices):
        CONSUMER = "consumer"
        BUSINESS = "business"
        ADMIN = "admin"
    role: CharField
    push_notifications_enabled: BooleanField
    email_notifications_enabled: BooleanField
```

From backend/apps/prices/tasks.py (check_price_alerts, line 46):
```python
# After alert fires, currently only logs. Plan 02-02 will hook notify dispatch here.
logger.info("notification_stub_triggered", user_id=..., alert_id=...)
```

Celery Beat schedule location: backend/config/settings/base.py → CELERY_BEAT_SCHEDULE dict
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds + BusinessProfile and Promotion models</name>
  <files>
    backend/tests/unit/test_business_models.py,
    backend/tests/integration/test_business_registration.py,
    backend/tests/integration/test_business_verification.py,
    backend/tests/integration/test_business_prices.py,
    backend/tests/integration/test_promotions.py,
    backend/apps/business/models.py,
    backend/apps/business/migrations/0001_initial.py,
    backend/apps/stores/models.py,
    backend/apps/stores/migrations/0002_store_business_profile.py,
    backend/apps/prices/models.py
  </files>
  <behavior>
    - test_business_models.py: test BusinessProfile fields (business_name, tax_id, address, website, is_verified=False default, rejection_reason blank, price_alert_threshold_pct=10); test Promotion UniqueConstraint raises IntegrityError on duplicate active promo for same product+store; test Promotion.DiscountType choices ('flat', 'percentage')
    - test_business_registration.py: POST /api/v1/business/profiles/ creates profile with is_verified=False; unauthenticated POST returns 401; non-business role returns 403
    - test_business_verification.py: admin POST /approve/ sets is_verified=True; admin POST /reject/ with reason sets rejection_reason; non-admin approve attempt returns 403
    - test_business_prices.py: verified business POST /api/v1/business/prices/ creates Price with source='business'; unverified business returns 403; price is_stale remains False (no expiry)
    - test_promotions.py: verified business creates promotion; deactivate sets is_active=False; second active promo same product+store returns 409; deactivate_expired_promotions task marks end_date-past promos inactive
  </behavior>
  <action>
    Create test scaffold files first (all tests fail — Wave 0 RED state is expected). Then implement:

    1. BusinessProfile model in backend/apps/business/models.py:
       - Fields: user (OneToOneField → settings.AUTH_USER_MODEL, related_name='business_profile'), business_name (CharField max_length=200), tax_id (CharField max_length=20, unique), address (TextField), website (URLField blank=True), is_verified (BooleanField default=False), rejection_reason (TextField blank=True), price_alert_threshold_pct (PositiveSmallIntegerField default=10), created_at (auto_now_add), updated_at (auto_now)
       - Meta: verbose_name="Perfil de Negocio"

    2. Promotion model in same file:
       - Fields: product (FK → products.Product, on_delete=CASCADE, related_name='promotions'), store (FK → stores.Store, on_delete=CASCADE, related_name='promotions'), discount_type (CharField max_length=20, choices=DiscountType.choices), discount_value (DecimalField max_digits=10, decimal_places=2), start_date (DateField), end_date (DateField null=True blank=True), is_active (BooleanField default=True), min_quantity (PositiveSmallIntegerField null=True blank=True), title (CharField max_length=100 blank=True), description (TextField blank=True), views (PositiveIntegerField default=0), created_at (auto_now_add)
       - DiscountType inner class: FLAT = "flat" / PERCENTAGE = "percentage"
       - Meta: constraints = [UniqueConstraint(fields=['product','store'], condition=Q(is_active=True), name='unique_active_promo_per_product_store')]
       - __str__: f"{self.discount_type} {self.discount_value} on {self.product.name} at {self.store.name}"

    3. Run: docker exec bargain-backend python manage.py makemigrations business → creates 0001_initial.py

    4. Add nullable FK to Store: business_profile = ForeignKey('business.BusinessProfile', null=True, blank=True, on_delete=SET_NULL, related_name='stores')
       IMPORTANT: BusinessProfile migration (0001_initial) must exist BEFORE this migration. Run makemigrations stores → creates 0002_store_business_profile.py

    5. Extend Price.Source: add BUSINESS = "business", "Portal PYME" to the TextChoices class. No migration needed (choices are validation only, not a DB constraint).

    Run migrations: docker exec bargain-backend python manage.py migrate

    Run tests (expect RED for integration tests, models tests should go GREEN after models exist):
    docker exec bargain-backend pytest tests/unit/test_business_models.py -v --tb=short
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml exec backend pytest tests/unit/test_business_models.py -v --tb=short</automated>
  </verify>
  <done>BusinessProfile and Promotion models exist and pass all unit model tests; Store.business_profile FK migration applied; Price.Source.BUSINESS choice present; all 5 test scaffold files exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Business portal ViewSets, permissions, Celery tasks, URL registration</name>
  <files>
    backend/apps/business/permissions.py,
    backend/apps/business/serializers.py,
    backend/apps/business/views.py,
    backend/apps/business/tasks.py,
    backend/apps/business/urls.py,
    backend/apps/prices/serializers.py,
    backend/config/settings/base.py,
    backend/apps/core/exceptions.py,
    backend/requirements/base.txt
  </files>
  <behavior>
    - IsVerifiedBusiness returns False (403) if user.role != 'business' or no verified BusinessProfile
    - BusinessProfileViewSet: business user can GET own profile; admin can approve (sets is_verified=True, fires email task) and reject (stores reason, fires email task); non-admin approve/reject returns 403
    - PromotionViewSet: verified business can CREATE, PATCH, DELETE(deactivate) promotions on their own stores; second active promo same product+store returns 409 with PromotionConflictError
    - BusinessPriceViewSet: verified business can POST new price with source='business' (no is_stale=True ever set by expiry tasks for this source)
    - promo_price field appears in GET /api/v1/prices/compare/ response when an active Promotion exists for that product+store
    - deactivate_expired_promotions: marks all Promotion(is_active=True, end_date__lt=today) as is_active=False
    - check_competitor_prices: compares scraped prices (source in SCRAPING, API) vs business prices for same product+store; emails business if diff > threshold_pct; never compares a business price against itself
  </behavior>
  <action>
    1. Add to backend/requirements/base.txt: `exponent_server_sdk>=2.0,<3.0`
       (This is the only new production dependency in Phase 2 — needed here so the Docker image rebuild picks it up before Plan 02-02.)

    2. Add to backend/apps/core/exceptions.py:
       - BusinessNotVerifiedError(BargainAPIException): status_code=403, default_code='business_not_verified'
       - PromotionConflictError(BargainAPIException): status_code=409, default_code='promotion_conflict'

    3. Create backend/apps/business/permissions.py:
       - IsVerifiedBusiness(BasePermission): checks request.user.is_authenticated AND user.role == 'business' AND BusinessProfile.objects.filter(user=request.user, is_verified=True).exists()
       - IsBusinessOwner(BasePermission): has_object_permission checks object.store.business_profile.user == request.user (for Promotions and BusinessPrices)

    4. Create backend/apps/business/serializers.py:
       - BusinessProfileSerializer: all fields including is_verified (read_only), rejection_reason (read_only for business, writable for admin)
       - PromotionSerializer: all fields; validated_data check that store.business_profile.user == request.user in validate()
       - BusinessPriceSerializer: wraps PriceSerializer (import from prices.serializers); source field forced to 'business' in create

    5. Create backend/apps/business/views.py:
       - BusinessProfileViewSet(ModelViewSet): queryset = BusinessProfile.objects.select_related('user'); get_permissions uses IsAdminUser for approve/reject actions, IsAuthenticated for others; filter_queryset to own profile for non-admin; approve action: profile.is_verified=True, profile.rejection_reason='', save, send_business_approval_email.delay(profile.id), return success_response; reject action: get reason from request.data, profile.is_verified=False, profile.rejection_reason=reason, save, send_business_rejection_email.delay(profile.id, reason), return success_response
       - PromotionViewSet(ModelViewSet): permission_classes=[IsAuthenticated, IsVerifiedBusiness]; on IntegrityError from UniqueConstraint raise PromotionConflictError; deactivate @action(detail=True, methods=['post']): sets is_active=False
       - BusinessPriceViewSet(ModelViewSet): permission_classes=[IsAuthenticated, IsVerifiedBusiness]; perform_create calls serializer.save(source=Price.Source.BUSINESS, is_stale=False); GET list scoped to stores owned by request.user's BusinessProfile

    6. Extend backend/apps/prices/serializers.py PriceComparisonSerializer (or PriceCompareView get()):
       In the view's GET handler, after fetching prices queryset, prefetch active promotions:
         promo_qs = Promotion.objects.filter(store_id__in=store_ids, is_active=True).select_related('product')
         promo_lookup = {(p.product_id, p.store_id): p for p in promo_qs}
       Pass promo_lookup as context to serializer. Add SerializerMethodField promo_price that looks up (product_id, store_id) in context['promo_lookup'] and computes discounted price. Add promotion field (PromotionMinimalSerializer).

    7. Create backend/apps/business/tasks.py:
       - send_business_approval_email(@shared_task bind=True ignore_result=True): fetches BusinessProfile.select_related('user'), calls send_mail subject="Tu negocio ha sido verificado en BargAIn"
       - send_business_rejection_email(@shared_task bind=True ignore_result=True): sends email with rejection reason
       - deactivate_expired_promotions(@shared_task bind=True ignore_result=True): Promotion.objects.filter(is_active=True, end_date__lt=timezone.now().date()).update(is_active=False)
       - check_competitor_prices(@shared_task bind=True ignore_result=True): for each verified BusinessProfile, get business prices (source=BUSINESS); for each, find latest scraped price (source in SCRAPING, API) same product+store; if abs(scraped-business)/business > threshold_pct/100: email business; store last_alert_at on profile to avoid repeated alerts within 24h

    8. Create backend/apps/business/urls.py using DefaultRouter, register BusinessProfileViewSet, PromotionViewSet, BusinessPriceViewSet.

    9. Add to backend/config/settings/base.py CELERY_BEAT_SCHEDULE:
       - "deactivate-expired-promotions-hourly": schedule=crontab(minute=5, hour='*'), task="apps.business.tasks.deactivate_expired_promotions"
       - "check-competitor-prices-daily": schedule=crontab(minute=0, hour=8), task="apps.business.tasks.check_competitor_prices"

    Run full integration test suite (RED state expected for some new tests until wiring complete, existing 179 tests must stay GREEN):
    docker exec bargain-backend pytest tests/ -v --tb=short
  </action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml exec backend pytest tests/integration/test_business_registration.py tests/integration/test_business_verification.py tests/integration/test_business_prices.py tests/integration/test_promotions.py -v --tb=short</automated>
  </verify>
  <done>All business portal integration tests green; existing 179 Phase 1 tests still green; promo_price appears in price comparison response; Celery Beat schedule updated; exponent_server_sdk added to requirements</done>
</task>

</tasks>

<verification>
Run full suite after both tasks complete:
docker compose -f docker-compose.dev.yml exec backend pytest tests/ -v --tb=short --cov=apps --cov-report=term

Check migration state:
docker compose -f docker-compose.dev.yml exec backend python manage.py showmigrations business stores

Check Promotion UniqueConstraint enforced at DB level:
docker compose -f docker-compose.dev.yml exec backend python manage.py shell -c "from django.db import connection; print([c for c in connection.introspection.get_constraints(connection.cursor(), 'business_promotion') if c == 'unique_active_promo_per_product_store'])"
</verification>

<success_criteria>
- All 5 Wave 0 test files exist and execute without import errors
- business/models.py: BusinessProfile and Promotion models with all specified fields
- business/permissions.py: IsVerifiedBusiness returns 403 for unverified business users
- business/views.py: approve/reject actions send email tasks; write endpoints return 403 for unverified
- prices/serializers.py: promo_price and promotion fields in price comparison response, no N+1
- Celery Beat schedule has deactivate_expired_promotions (hourly) and check_competitor_prices (daily)
- Phase 1 test suite (179 tests) remains fully green
- exponent_server_sdk>=2.0,<3.0 in requirements/base.txt
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-notifications/02-01-SUMMARY.md`
</output>
