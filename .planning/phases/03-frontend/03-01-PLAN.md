---
phase: 03-frontend
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/app.json
  - frontend/src/api/client.ts
  - frontend/src/api/authService.ts
  - frontend/src/api/listService.ts
  - frontend/src/api/productService.ts
  - frontend/src/api/storeService.ts
  - frontend/src/api/notificationService.ts
  - frontend/src/api/priceService.ts
  - frontend/src/store/authStore.ts
  - frontend/src/store/listStore.ts
  - frontend/src/store/notificationStore.ts
  - frontend/src/store/profileStore.ts
  - frontend/src/components/ui/SkeletonBox.tsx
  - frontend/src/components/ui/index.ts
  - frontend/src/navigation/types.ts
  - frontend/src/types/domain.ts
  - frontend/__tests__/apiClient.test.ts
  - frontend/__tests__/SkeletonBox.test.tsx
autonomous: true
requirements:
  - NFR-03

must_haves:
  truths:
    - "Axios client performs 401 → refresh → retry without logging out the user"
    - "Multiple concurrent 401s use the refresh queue; only one refresh call is made"
    - "Response interceptor unwraps {success, data} shape; JWT endpoints (flat objects) pass through unchanged"
    - "authStore persists tokens in expo-secure-store; app restores session after restart"
    - "SkeletonBox animates opacity 1→0.3→1 in a loop using Reanimated withRepeat"
    - "All API service modules export typed functions; screens never call apiClient directly"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "Upgraded Axios interceptor with refresh queue"
      exports: ["apiClient"]
    - path: "frontend/src/store/authStore.ts"
      provides: "Zustand auth store with refreshToken + hydrate"
      exports: ["useAuthStore", "User"]
    - path: "frontend/src/store/listStore.ts"
      provides: "Zustand lists store"
      exports: ["useListStore"]
    - path: "frontend/src/store/notificationStore.ts"
      provides: "Zustand notifications store"
      exports: ["useNotificationStore"]
    - path: "frontend/src/store/profileStore.ts"
      provides: "Zustand profile/preferences store"
      exports: ["useProfileStore"]
    - path: "frontend/src/components/ui/SkeletonBox.tsx"
      provides: "Animated skeleton loading placeholder"
      exports: ["SkeletonBox"]
    - path: "frontend/src/api/authService.ts"
      exports: ["authService"]
    - path: "frontend/src/api/listService.ts"
      exports: ["listService"]
    - path: "frontend/src/api/notificationService.ts"
      exports: ["notificationService"]
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "frontend/src/store/authStore.ts"
      via: "useAuthStore.getState().setToken() in refresh handler"
      pattern: "useAuthStore\\.getState\\(\\)"
    - from: "frontend/src/api/client.ts"
      to: "expo-secure-store"
      via: "SecureStore.getItemAsync('refresh_token') in 401 handler"
      pattern: "SecureStore\\.getItemAsync"
    - from: "App.tsx"
      to: "frontend/src/store/authStore.ts"
      via: "authStore.hydrate() called on app mount"
      pattern: "hydrate"
---

<objective>
Install missing native packages, upgrade the Axios client with JWT refresh-and-retry queue, create all Zustand stores and API service modules, add the SkeletonBox loading component, and extend navigation types. This is the foundation plan — every subsequent plan in Phase 3 depends on these contracts.

Purpose: Establish data-access and state-management infrastructure so screens can connect to real API data without implementing boilerplate individually.
Output: Upgraded apiClient, 4 Zustand stores, 6 API service files, SkeletonBox component, extended nav types, and 2 test scaffolds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend/03-CONTEXT.md
@.planning/phases/03-frontend/03-RESEARCH.md

@frontend/src/api/client.ts
@frontend/src/store/authStore.ts
@frontend/src/navigation/types.ts
@frontend/src/types/domain.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages, upgrade Axios client, and extend authStore</name>
  <files>
    frontend/package.json,
    frontend/app.json,
    frontend/src/api/client.ts,
    frontend/src/store/authStore.ts,
    frontend/__tests__/apiClient.test.ts
  </files>
  <behavior>
    - Test 1: 401 on a request triggers refresh token call, retries the original request, and does NOT call logout()
    - Test 2: Two simultaneous 401s result in exactly one refresh call (queue pattern)
    - Test 3: Response interceptor returns plain object for {success: true, data: {...}} responses
    - Test 4: Response interceptor returns flat object unchanged when response.data.success is undefined (JWT endpoint shape)
    - Test 5: authStore.hydrate() reads 'access_token' and 'refresh_token' from SecureStore and sets state
    - Test 6: authStore.login() writes tokens to SecureStore and sets isAuthenticated=true
    - Test 7: authStore.logout() clears SecureStore entries and resets state
  </behavior>
  <action>
    **Step 1 — Install missing packages (run from frontend/ directory):**
    ```
    cd frontend
    npx expo install expo-location react-native-maps react-native-gesture-handler
    ```

    **Step 2 — Update app.json:** Add expo-location config plugin under the "expo" key:
    ```json
    "plugins": [
      ["expo-location", {
        "locationAlwaysAndWhenInUsePermission": "BarGAIN necesita tu ubicación para mostrar tiendas cercanas."
      }]
    ]
    ```
    Add Android Google Maps key placeholder:
    ```json
    "android": {
      "config": {
        "googleMaps": {
          "apiKey": "$(EXPO_PUBLIC_GOOGLE_MAPS_KEY)"
        }
      }
    }
    ```

    **Step 3 — Upgrade frontend/src/api/client.ts** with the full refresh-and-retry pattern. KEEP the existing base URL and headers. ADD:
    - Module-level `let isRefreshing = false` and `let refreshQueue: Array<(token: string) => void> = []`
    - Upgrade response success interceptor: check `if (response.data?.success !== undefined) return response.data.data; return response.data;`
    - Upgrade response error interceptor: on 401 + !_retry, check isRefreshing flag. If refreshing → queue the request. If not refreshing → set flag, read refresh_token from SecureStore, call POST /auth/token/refresh/, update store + SecureStore, drain queue, retry. On refresh failure → call logout().
    - Import SecureStore from 'expo-secure-store'
    - The raw axios.post for /auth/token/refresh/ must use a plain `axios.create()` (not apiClient) to avoid interceptor recursion

    **Step 4 — Upgrade frontend/src/store/authStore.ts:** Add:
    - `refreshToken: string | null` field to AuthState interface
    - `memberSince: string | null` field (populated from backend user.date_joined)
    - `setRefreshToken: (token: string) => void` action
    - `hydrate: () => Promise<void>` action — reads 'access_token', 'refresh_token', 'auth_user' from SecureStore; if tokens exist, calls setToken + setRefreshToken + setUser + sets isAuthenticated=true
    - Update `login(token, refreshToken, user)` signature — writes both tokens to SecureStore, also writes user as JSON under 'auth_user'
    - Update `logout()` — deletes all three SecureStore keys
    - NOTE: Zustand 5 does NOT support built-in persist middleware with SecureStore. Use manual hydration — no middleware needed.

    **Step 5 — Create __tests__/apiClient.test.ts** as the Wave 0 test scaffold. Mock expo-secure-store and axios. Test the behaviors listed in the behavior block above. Tests should be RED before Step 3 is complete.
  </action>
  <verify>
    <automated>cd frontend && npx jest __tests__/apiClient.test.ts --bail</automated>
  </verify>
  <done>All 7 interceptor/store tests pass. authStore has refreshToken + hydrate fields. app.json has expo-location plugin. package.json has expo-location, react-native-maps, react-native-gesture-handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API service modules, Zustand stores (lists/notifications/profile), SkeletonBox, nav types</name>
  <files>
    frontend/src/api/authService.ts,
    frontend/src/api/listService.ts,
    frontend/src/api/productService.ts,
    frontend/src/api/storeService.ts,
    frontend/src/api/notificationService.ts,
    frontend/src/api/priceService.ts,
    frontend/src/store/listStore.ts,
    frontend/src/store/notificationStore.ts,
    frontend/src/store/profileStore.ts,
    frontend/src/components/ui/SkeletonBox.tsx,
    frontend/src/components/ui/index.ts,
    frontend/src/navigation/types.ts,
    frontend/__tests__/SkeletonBox.test.tsx
  </files>
  <behavior>
    - Test 1: SkeletonBox renders a View with width and height props applied
    - Test 2: SkeletonBox renders with accessible role or testID for query
    - Test 3: listStore.setLists() stores list array and getLists() returns same data
    - Test 4: notificationStore.unreadCount returns count of notifications where is_read=false
    - Test 5: notificationStore.markRead(id) sets is_read=true for matching notification
  </behavior>
  <action>
    **API Service Modules** — each in `frontend/src/api/`. All import from `./client` and use `apiClient`. Never call axios directly.

    `authService.ts`:
    - `login(email, password)` → POST /auth/token/
    - `register(data: {email, password, first_name, last_name})` → POST /auth/register/
    - `refreshToken(refresh: string)` → POST /auth/token/refresh/ (used internally by interceptor only)
    - `getProfile()` → GET /auth/profile/me/
    - `updateProfile(data)` → PATCH /auth/profile/me/
    - `updatePreferences(prefs)` → PATCH /auth/profile/me/preferences/
    - `changePassword(oldPassword, newPassword)` → PATCH /auth/profile/me/ with {old_password, new_password}
    - `requestPasswordReset(email)` → POST /auth/password-reset/

    `listService.ts`:
    - `getLists()` → GET /lists/
    - `getList(id)` → GET /lists/{id}/
    - `createList(name)` → POST /lists/
    - `updateList(id, data)` → PATCH /lists/{id}/
    - `deleteList(id)` → DELETE /lists/{id}/
    - `addItem(listId, {product_id, quantity})` → POST /lists/{id}/items/
    - `updateItem(listId, itemId, data)` → PATCH /lists/{id}/items/{itemId}/
    - `deleteItem(listId, itemId)` → DELETE /lists/{id}/items/{itemId}/

    `productService.ts`:
    - `search(query: string, page?: number)` → GET /products/?search={query}
    - `autocomplete(q: string)` → GET /products/autocomplete/?q={q}

    `storeService.ts`:
    - `getNearby(lat: number, lng: number, radius?: number)` → GET /stores/?lat={lat}&lng={lng}&radius={radius}

    `notificationService.ts`:
    - `getNotifications(page?: number)` → GET /notifications/?page={page}
    - `markAsRead(id: string)` → PATCH /notifications/{id}/read/
    - `markAllAsRead()` → PATCH /notifications/ with {mark_all_read: true}
    - `deleteNotification(id: string)` → DELETE /notifications/{id}/
    - `registerPushToken(token: string, deviceId: string)` → POST /notifications/push-token/ with {token, device_id}

    `priceService.ts`:
    - `getPriceAlerts()` → GET /prices/alerts/ (active alerts for current user)
    - `getPriceComparison(productId: string)` → GET /prices/?product={productId}

    **Zustand Stores:**

    `listStore.ts`: State: `lists: ShoppingList[]`, `activeList: ShoppingList | null`, `isLoading: boolean`. Actions: `setLists`, `setActiveList`, `addList`, `removeList`, `updateListItem`. Import ShoppingList from `@/types/domain`.

    `notificationStore.ts`: State: `notifications: Notification[]`, `unreadCount: number`, `isLoading: boolean`, `page: number`, `hasMore: boolean`. Actions: `setNotifications`, `appendNotifications`, `markRead(id)`, `markAllRead()`, `removeNotification(id)`. Interface `Notification` defined inline: `{id, notification_type, title, body, is_read, data, action_url, created_at}`.

    `profileStore.ts`: State: `profile: UserProfile | null`, `isLoading: boolean`, `isSaving: boolean`. Actions: `setProfile`, `updatePreferences(prefs)`. Import UserProfile from `@/types/domain`.

    **SkeletonBox component** at `frontend/src/components/ui/SkeletonBox.tsx`:
    - Props: `width: number | string`, `height: number`, `borderRadius?: number`, `style?: ViewStyle`
    - Uses Reanimated `useSharedValue` + `useAnimatedStyle` + `withRepeat` + `withTiming`
    - Animates opacity from 1.0 to 0.3 and back, 800ms duration, infinite loop
    - Background color: `colors.border` from theme
    - Border radius: default 8

    **Export SkeletonBox** from `frontend/src/components/ui/index.ts` alongside existing exports.

    **Update navigation types** at `frontend/src/navigation/types.ts`:
    - Add to `HomeStackParamList`: `Notifications: undefined`, `PriceAlerts: undefined`
    - Add to `ProfileStackParamList`: `ChangePassword: undefined`

    **Update domain types** at `frontend/src/types/domain.ts`:
    - Add `Notification` interface: `{id: string; notification_type: string; title: string; body: string; is_read: boolean; data: Record<string, unknown>; action_url: string | null; created_at: string}`
    - Add `PriceAlert` interface: `{id: string; product: Product; target_price: number; current_price: number; is_active: boolean; created_at: string}`
    - Add `UserPreferences` interface: `{max_search_radius_km: number; max_stops: number; weight_price: number; weight_distance: number; weight_time: number; push_notifications_enabled: boolean; notify_price_alerts: boolean; notify_new_promos: boolean; notify_shared_list_changes: boolean}`
  </action>
  <verify>
    <automated>cd frontend && npx jest __tests__/SkeletonBox.test.tsx __tests__/apiClient.test.ts --bail</automated>
  </verify>
  <done>All 6 API service files exist and export typed functions. 3 new Zustand stores created. SkeletonBox renders and is exported. Navigation types include Notifications and ChangePassword routes. Domain types include Notification, PriceAlert, UserPreferences. All tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx jest App.test.tsx --bail` passes (smoke test — no import errors)
- `cd frontend && npx jest __tests__/apiClient.test.ts __tests__/SkeletonBox.test.tsx --bail` all green
- `cd frontend && npx eslint src/api/ src/store/ src/components/ui/SkeletonBox.tsx` no errors
- `frontend/package.json` contains expo-location, react-native-maps, react-native-gesture-handler
- `frontend/app.json` has expo-location plugin with Spanish permission string
</verification>

<success_criteria>
- JWT interceptor: 401 → refresh → retry works; concurrent 401s queue correctly; only one refresh call
- authStore.hydrate() restores session from SecureStore on app restart
- All 6 API service modules exist and export typed functions
- SkeletonBox renders animated placeholder using Reanimated
- Navigation types include Notifications and ChangePassword routes
- App.test.tsx smoke test still passes (no broken imports)
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md`
</output>
