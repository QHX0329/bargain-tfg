---
phase: 05-optimizer-scraping
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/assistant/views.py
  - backend/apps/assistant/services.py
  - backend/apps/assistant/serializers.py
  - backend/apps/assistant/urls.py
  - tests/unit/test_assistant.py
  - tests/integration/test_assistant_api.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
must_haves:
  truths:
    - "POST /api/v1/assistant/chat/ accepts message history and returns an assistant response"
    - "System prompt constrains the assistant to shopping-domain queries only"
    - "Off-topic queries receive a polite rejection in Spanish"
    - "Message history is truncated to last 10 turns (20 messages) before sending to Claude API"
    - "The endpoint is rate-limited to prevent abuse"
  artifacts:
    - path: "backend/apps/assistant/services.py"
      provides: "Anthropic proxy with guardrails and history truncation"
      contains: "claude-haiku-4-5-20251001"
    - path: "backend/apps/assistant/views.py"
      provides: "AssistantChatView DRF endpoint"
      contains: "class AssistantChatView"
    - path: "backend/apps/assistant/serializers.py"
      provides: "Request/response serializers for chat"
      contains: "class ChatRequestSerializer"
    - path: "backend/apps/assistant/urls.py"
      provides: "URL route /chat/"
      contains: "chat/"
  key_links:
    - from: "backend/apps/assistant/views.py"
      to: "backend/apps/assistant/services.py"
      via: "AssistantChatView calls chat_with_assistant(messages)"
      pattern: "chat_with_assistant"
    - from: "backend/apps/assistant/services.py"
      to: "anthropic SDK"
      via: "client.messages.create with system prompt"
      pattern: "client\\.messages\\.create"
---

<objective>
Build the LLM assistant backend endpoint: POST /api/v1/assistant/chat/ that proxies user messages to the Claude API with shopping-domain guardrails, history truncation, and rate limiting.

Purpose: Provides an intelligent shopping assistant for price comparison, list suggestions, and recipe ideas. Per D-15 through D-18.
Output: Working assistant endpoint with Anthropic integration, guardrails, rate limiting, and tests.
</objective>

<execution_context>
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-optimizer-scraping/05-CONTEXT.md
@.planning/phases/05-optimizer-scraping/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/apps/core/exceptions.py:
- AssistantUnavailable (503) — already defined, use for Claude API errors

From backend/apps/assistant/urls.py (current stub):
- urlpatterns = [] — needs /chat/ route added

From backend/config/urls.py:
- path("api/v1/assistant/", include("apps.assistant.urls")) — already registered

From backend/config/settings/base.py:
- ANTHROPIC_API_KEY should be loaded from env (add to settings if not present)
- DEFAULT_THROTTLE_RATES already configured for DRF throttling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assistant service + serializers + view + URL + settings</name>
  <files>
    backend/apps/assistant/services.py,
    backend/apps/assistant/serializers.py,
    backend/apps/assistant/views.py,
    backend/apps/assistant/urls.py,
    backend/config/settings/base.py
  </files>
  <read_first>
    backend/apps/assistant/urls.py,
    backend/apps/assistant/apps.py,
    backend/apps/core/exceptions.py,
    backend/config/settings/base.py,
    backend/apps/prices/views.py
  </read_first>
  <action>
    **Per D-15, D-16, D-17, D-18:**

    1. **services.py** — `chat_with_assistant(messages: list[dict]) -> str`:
       - Define `SYSTEM_PROMPT` constant:
         ```
         Eres BarGAIN, un asistente de compra inteligente para Espana.
         Tu unica funcion es ayudar a los usuarios con: comparacion de precios de productos,
         sugerencias para su lista de la compra y recetas economicas con ingredientes disponibles.
         Si el usuario pregunta algo fuera de estos temas, responde amablemente:
         "Soy un asistente de compras. Puedo ayudarte con precios, listas o recetas. Tienes alguna pregunta sobre tu compra?"
         Responde siempre en espanol. Se conciso y util.
         ```
       - Truncate messages to last 20 entries (10 turns = 10 user + 10 assistant) per D-17
       - Create `anthropic.Anthropic(api_key=settings.ANTHROPIC_API_KEY)` client
       - Call `client.messages.create(model="claude-haiku-4-5-20251001", max_tokens=1024, system=SYSTEM_PROMPT, messages=truncated)`
       - Return `response.content[0].text`
       - Wrap API call in try/except: catch `anthropic.APIError`, `anthropic.APIConnectionError`, `anthropic.RateLimitError` and raise `AssistantUnavailable` with appropriate message
       - Log all calls with structlog (user message count, model, truncated flag)

    2. **serializers.py**:

       `MessageSerializer(serializers.Serializer)`:
       - `role = serializers.ChoiceField(choices=["user", "assistant"])`
       - `content = serializers.CharField(max_length=4000)`

       `ChatRequestSerializer(serializers.Serializer)`:
       - `messages = MessageSerializer(many=True, min_length=1)`
       - Custom validate: last message must have `role="user"`, otherwise raise ValidationError

       `ChatResponseSerializer(serializers.Serializer)`:
       - `role = serializers.CharField(default="assistant")`
       - `content = serializers.CharField()`

    3. **views.py** — `AssistantChatView(APIView)`:
       - `permission_classes = [IsAuthenticated]`
       - `throttle_scope = "assistant"`
       - `post(self, request)`:
         - Validate with `ChatRequestSerializer(data=request.data)`
         - Extract validated messages list
         - Call `chat_with_assistant(messages)`
         - Return `Response({"success": True, "data": {"role": "assistant", "content": response_text}})`
         - Catch `AssistantUnavailable` and return 503 with standard error format

    4. **urls.py** — Replace empty urlpatterns:
       ```python
       from django.urls import path
       from .views import AssistantChatView

       urlpatterns = [
           path("chat/", AssistantChatView.as_view(), name="assistant-chat"),
       ]
       ```

    5. **base.py** — Add to settings (if not already present):
       - `ANTHROPIC_API_KEY = env("ANTHROPIC_API_KEY", default="")` in the API keys section
       - In `DEFAULT_THROTTLE_RATES` dict, add: `"assistant": "30/hour"` and `"ocr": "60/hour"`
       - Add `ANTHROPIC_API_KEY` to `.env.example` as well (empty placeholder)
  </action>
  <verify>
    <automated>docker exec bargain-backend python -c "from apps.assistant.views import AssistantChatView; from apps.assistant.services import chat_with_assistant, SYSTEM_PROMPT; print('Assistant imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/apps/assistant/services.py contains "def chat_with_assistant" and "claude-haiku-4-5-20251001" and "SYSTEM_PROMPT" and "messages[-20:]"
    - backend/apps/assistant/serializers.py contains "class ChatRequestSerializer" and "class MessageSerializer" and "class ChatResponseSerializer"
    - backend/apps/assistant/views.py contains "class AssistantChatView" and "IsAuthenticated" and "throttle_scope" and "chat_with_assistant"
    - backend/apps/assistant/urls.py contains 'path("chat/"' and "AssistantChatView"
    - backend/config/settings/base.py contains "ANTHROPIC_API_KEY" and '"assistant"' in throttle rates
  </acceptance_criteria>
  <done>POST /api/v1/assistant/chat/ proxies to Claude API with system prompt guardrails, 10-turn truncation, and rate limiting</done>
</task>

<task type="auto">
  <name>Task 2: Assistant unit + integration tests</name>
  <files>
    tests/unit/test_assistant.py,
    tests/integration/test_assistant_api.py
  </files>
  <read_first>
    backend/apps/assistant/services.py,
    backend/apps/assistant/views.py,
    backend/apps/assistant/serializers.py
  </read_first>
  <action>
    1. **tests/unit/test_assistant.py** — Unit tests for service layer:
       - `test_chat_truncates_to_20_messages`: create 30 messages, mock anthropic client, call `chat_with_assistant(messages)`, assert the `messages` kwarg passed to `client.messages.create` has exactly 20 entries
       - `test_chat_uses_correct_model`: mock anthropic client, call `chat_with_assistant([{"role": "user", "content": "hola"}])`, assert `model="claude-haiku-4-5-20251001"` in the create call
       - `test_chat_includes_system_prompt`: mock anthropic client, call function, assert `system=SYSTEM_PROMPT` in the create call
       - `test_chat_raises_assistant_unavailable_on_api_error`: mock anthropic client to raise `anthropic.APIError`, assert `AssistantUnavailable` raised
       - `test_system_prompt_mentions_compras`: assert "compra" in SYSTEM_PROMPT.lower() (guardrail present)

    2. **tests/integration/test_assistant_api.py** — API integration tests:
       - Use `APIClient` with authenticated user
       - `test_chat_endpoint_returns_200`: mock `chat_with_assistant` to return "Hola, puedo ayudarte", POST to `/api/v1/assistant/chat/` with `{"messages": [{"role": "user", "content": "Hola"}]}`, assert 200 and response has `success: True` and `data.content`
       - `test_chat_endpoint_requires_auth`: POST without token, assert 401
       - `test_chat_rejects_empty_messages`: POST with `{"messages": []}`, assert 400
       - `test_chat_rejects_last_message_not_user`: POST with `{"messages": [{"role": "assistant", "content": "..."}]}`, assert 400
       - `test_chat_returns_503_on_service_unavailable`: mock `chat_with_assistant` to raise `AssistantUnavailable`, assert 503
  </action>
  <verify>
    <automated>docker exec bargain-backend pytest tests/unit/test_assistant.py tests/integration/test_assistant_api.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_assistant.py contains "test_chat_truncates_to_20_messages" and "test_chat_uses_correct_model" and "test_chat_raises_assistant_unavailable"
    - tests/integration/test_assistant_api.py contains "test_chat_endpoint_returns_200" and "test_chat_endpoint_requires_auth" and "test_chat_returns_503"
    - pytest exits 0 for both test files
  </acceptance_criteria>
  <done>All assistant unit tests (mock Anthropic SDK) and integration tests (mock service, real DRF endpoint) pass</done>
</task>

</tasks>

<verification>
- `docker exec bargain-backend pytest tests/unit/test_assistant.py tests/integration/test_assistant_api.py -v --tb=short` all green
- `grep "SYSTEM_PROMPT" backend/apps/assistant/services.py` confirms guardrail prompt exists
- `grep "throttle_scope" backend/apps/assistant/views.py` confirms rate limiting
</verification>

<success_criteria>
POST /api/v1/assistant/chat/ accepts message history, proxies to Claude Haiku with shopping-domain guardrails, truncates to 10 turns, is rate-limited at 30/hour. All unit and integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimizer-scraping/05-03-SUMMARY.md`
</output>
