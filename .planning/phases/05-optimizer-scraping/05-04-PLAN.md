---
phase: 05-optimizer-scraping
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docker-compose.dev.yml
  - backend/apps/optimizer/models.py
  - backend/apps/optimizer/serializers.py
  - backend/apps/optimizer/views.py
  - backend/apps/optimizer/urls.py
  - backend/apps/optimizer/services/__init__.py
  - backend/apps/optimizer/services/distance.py
  - backend/apps/optimizer/services/solver.py
  - backend/apps/optimizer/migrations/0001_initial.py
  - tests/unit/test_optimizer.py
  - tests/integration/test_optimizer_api.py
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03
  - OPT-04
  - NFR-01
must_haves:
  truths:
    - "POST /api/v1/optimize/ accepts a shopping list, user location, weights, and max_stops and returns an optimized route"
    - "The scoring function applies user weights to normalized price savings, distance, and time"
    - "OR-Tools TSP solver computes the optimal store visit order with max_stops constraint"
    - "Graphhopper provides real road distances/times between stores (fallback to haversine if unavailable)"
    - "OptimizationResult is persisted in DB with route_data JSONB"
    - "When no stores exist in radius, API returns 404 with OPTIMIZER_NO_STORES_IN_RADIUS code"
  artifacts:
    - path: "backend/apps/optimizer/models.py"
      provides: "OptimizationResult model with route_data JSONB"
      contains: "class OptimizationResult"
    - path: "backend/apps/optimizer/services/solver.py"
      provides: "OR-Tools TSP wrapper with multicriteria scoring"
      contains: "RoutingModel"
    - path: "backend/apps/optimizer/services/distance.py"
      provides: "Graphhopper matrix client with haversine fallback"
      contains: "get_distance_matrix"
    - path: "backend/apps/optimizer/views.py"
      provides: "OptimizeView DRF endpoint"
      contains: "class OptimizeView"
    - path: "docker-compose.dev.yml"
      provides: "Graphhopper service for road routing"
      contains: "graphhopper"
  key_links:
    - from: "backend/apps/optimizer/views.py"
      to: "backend/apps/optimizer/services/solver.py"
      via: "OptimizeView calls solve_route"
      pattern: "solve_route"
    - from: "backend/apps/optimizer/services/solver.py"
      to: "backend/apps/optimizer/services/distance.py"
      via: "solver calls get_distance_matrix for road distances"
      pattern: "get_distance_matrix"
    - from: "backend/apps/optimizer/services/distance.py"
      to: "Graphhopper container"
      via: "HTTP POST to graphhopper:8989/matrix"
      pattern: "graphhopper.*matrix"
    - from: "backend/apps/optimizer/views.py"
      to: "backend/apps/optimizer/models.py"
      via: "Persists OptimizationResult after solving"
      pattern: "OptimizationResult\\.objects\\.create"
---

<objective>
Build the Graphhopper routing service (Docker), the multicriteria optimizer algorithm using OR-Tools, the OptimizationResult model, and the POST /api/v1/optimize/ endpoint. This is the core algorithmic feature of BarGAIN.

Purpose: Enables users to find the optimal combination of stores that minimizes a weighted function of price, distance, and time. Per D-05 through D-10.
Output: Graphhopper container, distance matrix service, OR-Tools solver, optimizer endpoint, model, migration, and tests.
</objective>

<execution_context>
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-optimizer-scraping/05-CONTEXT.md
@.planning/phases/05-optimizer-scraping/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/apps/stores/models.py:
- Store model with: id, name, chain (FK), address, location (PostGIS PointField), opening_hours (JSON), is_local_business

From backend/apps/shopping_lists/models.py:
- ShoppingList model with: id, user (FK), name
- ShoppingListItem model with: product (FK), quantity, is_checked

From backend/apps/prices/models.py:
- Price model with: product (FK), store (FK), price, unit_price, offer_price, source, verified_at

From backend/apps/core/exceptions.py:
- OptimizationError (422) — already defined
- StoreNotFoundError (404) — use for OPTIMIZER_NO_STORES_IN_RADIUS

From CLAUDE.md — OptimizationResult model spec:
- id, shopping_list (FK), user_location (PostGIS), max_distance_km, optimization_mode, total_price, total_distance_km, estimated_time_minutes, route_data (JSONB)

From CLAUDE.md — scoring formula:
- Score = w_precio * ahorro_normalizado - w_distancia * distancia_extra_normalizada - w_tiempo * tiempo_extra_normalizado

From backend/apps/optimizer/urls.py (current stub):
- urlpatterns = [] — needs /optimize/ route (note: parent already at /api/v1/optimize/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graphhopper Docker + OptimizationResult model + distance service + solver</name>
  <files>
    docker-compose.dev.yml,
    backend/apps/optimizer/models.py,
    backend/apps/optimizer/services/__init__.py,
    backend/apps/optimizer/services/distance.py,
    backend/apps/optimizer/services/solver.py
  </files>
  <read_first>
    docker-compose.dev.yml,
    backend/apps/optimizer/urls.py,
    backend/apps/optimizer/apps.py,
    backend/apps/stores/models.py,
    backend/apps/shopping_lists/models.py,
    backend/apps/prices/models.py,
    backend/apps/core/exceptions.py
  </read_first>
  <action>
    **Per D-05, D-06, D-07, D-08, D-10:**

    1. **docker-compose.dev.yml** — Add `graphhopper` service at the end of the services block:
       ```yaml
       graphhopper:
         image: israelhikingmap/graphhopper:latest
         container_name: bargain-graphhopper
         ports:
           - "8989:8989"
         volumes:
           - graphhopper-data:/data
         command: >
           --url https://download.geofabrik.de/europe/spain/andalucia-latest.osm.pbf
           --host 0.0.0.0 --port 8989
         healthcheck:
           test: ["CMD", "curl", "-f", "http://localhost:8989/health"]
           interval: 30s
           timeout: 10s
           retries: 10
           start_period: 120s
       ```
       Add `graphhopper-data:` to the top-level `volumes:` section.
       Add `depends_on: graphhopper: condition: service_healthy` to the backend service (if feasible, otherwise just add the service standalone).

    2. **models.py** — `OptimizationResult(models.Model)`:
       - `shopping_list = models.ForeignKey("shopping_lists.ShoppingList", on_delete=models.CASCADE, related_name="optimizations")`
       - `user_location = gis_models.PointField(srid=4326, help_text="Ubicacion del usuario al optimizar")`
       - `max_distance_km = models.FloatField(default=10.0)`
       - `max_stops = models.IntegerField(default=3, validators=[MinValueValidator(2), MaxValueValidator(5)])`
       - `optimization_mode = models.CharField(max_length=20, choices=[("precio", "Precio"), ("distancia", "Distancia"), ("balanced", "Equilibrado")], default="balanced")`
       - `w_precio = models.FloatField(default=0.5)`
       - `w_distancia = models.FloatField(default=0.3)`
       - `w_tiempo = models.FloatField(default=0.2)`
       - `total_price = models.DecimalField(max_digits=10, decimal_places=2)`
       - `total_distance_km = models.FloatField()`
       - `estimated_time_minutes = models.FloatField()`
       - `route_data = models.JSONField(help_text="Paradas ordenadas con detalle por tienda")`
       - `created_at = models.DateTimeField(auto_now_add=True)`
       - `class Meta: ordering = ["-created_at"]`

    3. **services/__init__.py** — empty (package marker)

    4. **services/distance.py** — `get_distance_matrix(points: list[tuple[float, float]], graphhopper_url: str = None) -> tuple[list[list[float]], list[list[float]]]`:
       - Default `graphhopper_url` from `settings.GRAPHHOPPER_URL` (add `GRAPHHOPPER_URL = env("GRAPHHOPPER_URL", default="http://graphhopper:8989")` to base.py)
       - POST to `{graphhopper_url}/matrix` with payload:
         ```python
         {"from_points": [[lng, lat] for lat, lng in points],
          "to_points": [[lng, lat] for lat, lng in points],
          "out_arrays": ["distances", "times"],
          "vehicle": "car"}
         ```
         **IMPORTANT:** Graphhopper expects `[lng, lat]` order, NOT `[lat, lng]`.
       - Parse response: `distances` (meters -> km by /1000), `times` (seconds -> minutes by /60)
       - Return `(distance_matrix_km, time_matrix_minutes)`
       - **Fallback:** If Graphhopper request fails (ConnectionError, Timeout, non-200), log warning with structlog and compute haversine distances using PostGIS `ST_Distance`:
         - For each pair of points, compute `GEOSGeometry(Point(lng, lat, srid=4326)).transform(3857)` distance
         - Estimate time as `distance_km / 40 * 60` (avg urban speed 40 km/h)
       - `timeout=10` on the HTTP request

    5. **services/solver.py** — `solve_route(...)` function:
       - Input: `distance_matrix: list[list[float]], time_matrix: list[list[float]], price_savings: list[float], weights: dict[str, float], max_stops: int`
       - Create `RoutingIndexManager(n, 1, 0)` — n locations, 1 vehicle, depot=index 0 (user location)
       - Create `RoutingModel(manager)`
       - Register arc cost callback per CLAUDE.md formula:
         ```python
         def arc_cost(from_idx, to_idx):
             from_node = manager.IndexToNode(from_idx)
             to_node = manager.IndexToNode(to_idx)
             dist_score = weights["distancia"] * distance_matrix[from_node][to_node]
             time_score = weights["tiempo"] * time_matrix[from_node][to_node]
             price_bonus = weights["precio"] * price_savings[to_node]
             return int((dist_score + time_score - price_bonus) * 1000)
         ```
       - Add stop count dimension: `routing.AddDimension(count_idx, 0, max_stops, True, "stop_count")`
       - Search params: `first_solution_strategy = PATH_CHEAPEST_ARC`, `time_limit.seconds = 5`
       - Extract solution route (list of node indices), return empty list if no solution

       Also create `optimize_shopping_list(shopping_list_id: int, user_lat: float, user_lng: float, max_distance_km: float, max_stops: int, weights: dict) -> dict`:
       - Query `ShoppingListItem.objects.filter(shopping_list_id=shopping_list_id, is_checked=False).select_related("product")`
       - Query candidate stores within radius using PostGIS: `Store.objects.filter(location__distance_lte=(user_point, D(km=max_distance_km)))`
       - If no stores: raise `StoreNotFoundError` with code `OPTIMIZER_NO_STORES_IN_RADIUS`
       - For each candidate store, query `Price.objects.filter(product__in=products, store=store, source="scraping")` to get latest prices
       - Filter to stores that carry at least 1 product from the list
       - Build points list: `[user_location] + [store.location for store in candidate_stores]`
       - Call `get_distance_matrix(points)`
       - Normalize: price_savings per store = sum of (max_price - store_price) for each product; normalize 0-1
       - Normalize distance and time matrices 0-1
       - Call `solve_route(dist_matrix, time_matrix, price_savings, weights, max_stops)`
       - Build route_data: ordered list of `{"store_id": ..., "store_name": ..., "chain": ..., "lat": ..., "lng": ..., "distance_km": ..., "time_minutes": ..., "products": [{"product_id": ..., "name": ..., "price": ...}]}`
       - Compute totals: `total_price`, `total_distance_km`, `estimated_time_minutes`
       - Return result dict
  </action>
  <verify>
    <automated>docker exec bargain-backend python -c "from apps.optimizer.models import OptimizationResult; from apps.optimizer.services.solver import solve_route, optimize_shopping_list; from apps.optimizer.services.distance import get_distance_matrix; print('Optimizer imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.dev.yml contains "graphhopper" service and "israelhikingmap/graphhopper" and "andalucia-latest.osm.pbf" and "graphhopper-data"
    - backend/apps/optimizer/models.py contains "class OptimizationResult" and "route_data" and "JSONField" and "PointField" and "max_stops"
    - backend/apps/optimizer/services/distance.py contains "def get_distance_matrix" and "graphhopper" and "matrix" and "ST_Distance" (fallback)
    - backend/apps/optimizer/services/solver.py contains "def solve_route" and "RoutingModel" and "RoutingIndexManager" and "def optimize_shopping_list" and "OPTIMIZER_NO_STORES_IN_RADIUS"
    - backend/config/settings/base.py contains "GRAPHHOPPER_URL"
  </acceptance_criteria>
  <done>Graphhopper Docker service configured, OptimizationResult model created, distance matrix service with haversine fallback, OR-Tools solver with multicriteria scoring, optimize_shopping_list orchestrator function</done>
</task>

<task type="auto">
  <name>Task 2: Optimizer serializers + view + URL + migration + tests</name>
  <files>
    backend/apps/optimizer/serializers.py,
    backend/apps/optimizer/views.py,
    backend/apps/optimizer/urls.py,
    tests/unit/test_optimizer.py,
    tests/integration/test_optimizer_api.py
  </files>
  <read_first>
    backend/apps/optimizer/models.py,
    backend/apps/optimizer/services/solver.py,
    backend/apps/optimizer/services/distance.py,
    backend/apps/optimizer/urls.py,
    backend/apps/core/exceptions.py,
    backend/apps/stores/models.py
  </read_first>
  <action>
    **Per D-07, D-08, D-09, D-10:**

    1. **serializers.py**:

       `OptimizeRequestSerializer(serializers.Serializer)`:
       - `shopping_list_id = serializers.IntegerField()`
       - `lat = serializers.FloatField(help_text="User latitude")`
       - `lng = serializers.FloatField(help_text="User longitude")`
       - `max_distance_km = serializers.FloatField(default=10.0, min_value=1.0, max_value=50.0)`
       - `max_stops = serializers.IntegerField(default=3, min_value=2, max_value=5)`
       - `w_precio = serializers.FloatField(default=0.5, min_value=0, max_value=1)`
       - `w_distancia = serializers.FloatField(default=0.3, min_value=0, max_value=1)`
       - `w_tiempo = serializers.FloatField(default=0.2, min_value=0, max_value=1)`
       - Custom validate: normalize weights so they sum to 1.0 (`w / total` for each)

       `RouteStopSerializer(serializers.Serializer)`:
       - `store_id`, `store_name`, `chain`, `lat`, `lng`, `distance_km`, `time_minutes`, `products` (list)

       `OptimizeResponseSerializer(serializers.Serializer)`:
       - `id = serializers.IntegerField()` (OptimizationResult PK)
       - `total_price = serializers.DecimalField(max_digits=10, decimal_places=2)`
       - `total_distance_km = serializers.FloatField()`
       - `estimated_time_minutes = serializers.FloatField()`
       - `route = RouteStopSerializer(many=True, source="route_data")`

    2. **views.py** — `OptimizeView(APIView)`:
       - `permission_classes = [IsAuthenticated]`
       - `post(self, request)`:
         - Validate with `OptimizeRequestSerializer(data=request.data)`
         - Verify shopping list belongs to `request.user` (or 404)
         - Call `optimize_shopping_list(shopping_list_id, lat, lng, max_distance_km, max_stops, weights={"precio": w_p, "distancia": w_d, "tiempo": w_t})`
         - Create `OptimizationResult` with all fields + `user_location = Point(lng, lat, srid=4326)`
         - Serialize with `OptimizeResponseSerializer`
         - Return `Response({"success": True, "data": serializer.data})`
         - Catch `StoreNotFoundError`: return 404 with `{"success": False, "error": {"code": "OPTIMIZER_NO_STORES_IN_RADIUS", "message": "No hay tiendas en tu radio de busqueda. Prueba ampliando el radio."}}`
         - Catch `OptimizationError`: return 422

    3. **urls.py** — Replace empty urlpatterns:
       ```python
       from django.urls import path
       from .views import OptimizeView

       urlpatterns = [
           path("", OptimizeView.as_view(), name="optimize"),
       ]
       ```
       (Note: parent URL is `/api/v1/optimize/` so this catches POST to `/api/v1/optimize/`)

    4. **Run migration**: `python manage.py makemigrations optimizer` to create 0001_initial.py, then `python manage.py migrate optimizer`

    5. **tests/unit/test_optimizer.py**:
       - `test_solve_route_returns_ordered_nodes`: build a 4x4 distance/time matrix with known values, call `solve_route(...)`, assert result is a non-empty list of node indices starting from 0
       - `test_solve_route_respects_max_stops`: set max_stops=2, assert len(route) <= 3 (depot + 2 stops)
       - `test_solve_route_empty_when_no_solution`: pass incompatible constraints, assert empty list returned
       - `test_weight_normalization_in_serializer`: create `OptimizeRequestSerializer(data={"w_precio": 50, "w_distancia": 30, "w_tiempo": 20, ...})`, validate, assert weights sum to 1.0
       - `test_optimize_raises_no_stores`: mock Store queryset to return empty, call `optimize_shopping_list(...)`, assert `StoreNotFoundError`

    6. **tests/integration/test_optimizer_api.py**:
       - `test_optimize_endpoint_returns_200`: create ShoppingList + Items + Stores + Prices fixtures, mock `get_distance_matrix` to return fixed matrices, POST to `/api/v1/optimize/`, assert 200, response has `total_price`, `route`
       - `test_optimize_requires_auth`: POST without token, assert 401
       - `test_optimize_404_no_stores_in_radius`: create ShoppingList with items but no stores nearby, assert 404 with `OPTIMIZER_NO_STORES_IN_RADIUS`
       - `test_optimize_validates_max_stops_range`: POST with `max_stops=10`, assert 400
  </action>
  <verify>
    <automated>docker exec bargain-backend pytest tests/unit/test_optimizer.py tests/integration/test_optimizer_api.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - backend/apps/optimizer/serializers.py contains "class OptimizeRequestSerializer" and "class OptimizeResponseSerializer" and "max_stops" and "min_value=2" and "max_value=5"
    - backend/apps/optimizer/views.py contains "class OptimizeView" and "optimize_shopping_list" and "OPTIMIZER_NO_STORES_IN_RADIUS" and "IsAuthenticated"
    - backend/apps/optimizer/urls.py contains "OptimizeView"
    - tests/unit/test_optimizer.py contains "test_solve_route_returns_ordered_nodes" and "test_solve_route_respects_max_stops"
    - tests/integration/test_optimizer_api.py contains "test_optimize_endpoint_returns_200" and "test_optimize_404_no_stores"
    - pytest exits 0 for both test files
    - OptimizationResult migration exists in backend/apps/optimizer/migrations/
  </acceptance_criteria>
  <done>POST /api/v1/optimize/ validates request, runs OR-Tools solver with Graphhopper distances, persists OptimizationResult, returns route with stops. 404 when no stores in radius. All tests pass.</done>
</task>

</tasks>

<verification>
- `docker exec bargain-backend pytest tests/unit/test_optimizer.py tests/integration/test_optimizer_api.py -v --tb=short` all green
- `docker exec bargain-backend python manage.py showmigrations optimizer` shows 0001_initial applied
- `grep "graphhopper" docker-compose.dev.yml` confirms service present
- `docker compose -f docker-compose.dev.yml config --services` includes graphhopper
</verification>

<success_criteria>
Graphhopper container routes real road distances in Andalucia. POST /api/v1/optimize/ accepts shopping list + location + weights + max_stops, runs OR-Tools multicriteria solver, persists result, returns ordered route with per-store product breakdown. Haversine fallback when Graphhopper is down. 404 with OPTIMIZER_NO_STORES_IN_RADIUS when applicable. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimizer-scraping/05-04-SUMMARY.md`
</output>
