---
phase: 07-testing-deploy-memoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/optimizer/services/distance.py
  - config/settings/base.py
  - config/settings/prod.py
  - .env.example
  - render.yaml
  - tests/unit/test_distance_ors.py
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - NFR-02
user_setup:
  - service: openrouteservice
    why: "Matriz de distancias para el optimizador en staging (sin Graphhopper)"
    env_vars:
      - name: ORS_API_KEY
        source: "openrouteservice.org → Dashboard → API Keys → Create API Key (plan gratuito)"
  - service: render
    why: "Hosting del backend de staging"
    env_vars:
      - name: SECRET_KEY
        source: "Generar con: python -c \"import secrets; print(secrets.token_urlsafe(50))\""
      - name: ALLOWED_HOSTS
        source: "URL del Web Service de Render tras crearlo, ej: bargain-api.onrender.com"
      - name: ORS_API_KEY
        source: "openrouteservice.org (ver arriba)"
      - name: GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console → Credentials"
      - name: GEMINI_API_KEY
        source: "Google AI Studio → API Keys"
      - name: GOOGLE_CLOUD_VISION_API_KEY
        source: "Google Cloud Console → Credentials → Vision API"

must_haves:
  truths:
    - "El optimizador llama a ORS en vez de Graphhopper cuando hay ORS_API_KEY configurada"
    - "El backend arranca en Render sin redirect loops de SSL"
    - "La DATABASE_URL de Render se convierte correctamente a formato postgis://"
    - "render.yaml define los 5 servicios: Web, PostgreSQL, Redis, worker, beat"
  artifacts:
    - path: "backend/apps/optimizer/services/distance.py"
      provides: "Cliente ORS para la matriz de distancias"
      contains: "api.openrouteservice.org"
    - path: "config/settings/base.py"
      provides: "Setting ORS_API_KEY"
      contains: "ORS_API_KEY"
    - path: "config/settings/prod.py"
      provides: "Fix SECURE_PROXY_SSL_HEADER"
      contains: "SECURE_PROXY_SSL_HEADER"
    - path: "render.yaml"
      provides: "Declaración infraestructura Render"
      contains: "services:"
    - path: "tests/unit/test_distance_ors.py"
      provides: "Test unitario del cliente ORS"
      exports: ["test_ors_matrix_success", "test_ors_fallback_on_error"]
  key_links:
    - from: "backend/apps/optimizer/services/distance.py"
      to: "https://api.openrouteservice.org/v2/matrix/driving-car"
      via: "requests.post con Authorization header"
      pattern: "openrouteservice.org"
    - from: "config/settings/prod.py"
      to: "Render HTTPS proxy"
      via: "SECURE_PROXY_SSL_HEADER"
      pattern: "HTTP_X_FORWARDED_PROTO"
---

<objective>
Integrar OpenRouteService (ORS) como proveedor de la matriz de distancias del optimizador,
reemplazando Graphhopper que no se despliega en Render (D-04). Aplicar los dos fixes de deploy
críticos para que el backend arranque correctamente en Render: conversión del prefijo de
DATABASE_URL (postgresql:// → postgis://) y SECURE_PROXY_SSL_HEADER para evitar redirect loops.
Crear render.yaml con los 5 servicios declarados. Añadir test unitario del nuevo cliente ORS.

Purpose: Sin este plan no se puede desplegar el backend en Render ni ejecutar la demo de defensa.
ORS es el único proveedor de rutas realistas viable en staging gratuito (D-04).
Output: distance.py migrado a ORS, settings corregidas para Render, render.yaml creado, test verde.
</objective>

<execution_context>
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-testing-deploy-memoria/07-CONTEXT.md
@.planning/phases/07-testing-deploy-memoria/07-RESEARCH.md

<interfaces>
<!-- backend/apps/optimizer/services/distance.py — función a migrar -->
<!-- Signatura actual (mantener compatible): -->
```python
def get_distance_matrix(
    points: list[tuple[float, float]],
    graphhopper_url: str | None = None
) -> tuple[list[list[float]], list[list[float]]]:
    """Retorna (distance_matrix_km, time_matrix_min)."""
```

<!-- Configuración en config/settings/base.py — añadir: -->
```python
ORS_API_KEY: str = os.environ.get("ORS_API_KEY", "")
```

<!-- ORS Matrix API (desde RESEARCH.md §3): -->
# POST https://api.openrouteservice.org/v2/matrix/driving-car
# Headers: Authorization: {ORS_API_KEY}
# Body: {"locations": [[lng, lat], ...], "metrics": ["distance", "duration"], "units": "km"}
# Response: {"durations": [[...]], "distances": [[...]]}
# durations en segundos → dividir /60 para minutos
# distances en km (units="km") → NO dividir
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar distance.py de Graphhopper a ORS</name>
  <files>
    backend/apps/optimizer/services/distance.py,
    config/settings/base.py,
    .env.example,
    tests/unit/test_distance_ors.py
  </files>
  <behavior>
    - Test 1: get_distance_matrix([[0,0],[1,1]]) con ORS mock retorna tupla (dist_km, time_min) correctas
    - Test 2: response["durations"] /60 da minutos; response["distances"] en km sin convertir
    - Test 3: ConnectionError en ORS → fallback haversine (comportamiento existente preservado)
    - Test 4: ORS_API_KEY vacío → fallback haversine (no crashea con Authorization: "")
  </behavior>
  <action>
    1. Leer el archivo actual `backend/apps/optimizer/services/distance.py` completo para entender la función `get_distance_matrix`.

    2. En `config/settings/base.py` añadir al final de la sección de env vars:
       ```python
       ORS_API_KEY = os.environ.get("ORS_API_KEY", "")
       ```

    3. En `.env.example` añadir:
       ```
       ORS_API_KEY=your-ors-api-key
       ```

    4. Reescribir la función `get_distance_matrix` en `distance.py`:
       - Reemplazar parámetro `graphhopper_url=None` por `ors_api_key=None` (backward compat: si se llama desde solver.py sin el param, lee de settings).
       - Leer API key: `api_key = ors_api_key or settings.ORS_API_KEY`
       - Si `api_key` está vacío, llamar directamente a `_fallback_matrices(points)` y retornar.
       - Endpoint: `POST https://api.openrouteservice.org/v2/matrix/driving-car`
       - Headers: `{"Authorization": api_key, "Content-Type": "application/json"}`
       - Payload: `{"locations": points, "metrics": ["distance", "duration"], "units": "km"}`
         (points ya son [[lng,lat],...] — mismo orden que Graphhopper, sin cambio)
       - Response parsing:
         - `distances = data["distances"]` (ya en km, sin dividir por 1000)
         - `times = [[sec/60 for sec in row] for row in data["durations"]]`
       - Mantener try/except con fallback haversine para ConnectionError, Timeout, HTTPError.
       - Actualizar docstring indicando que usa ORS API.
       - Mantener las type hints existentes.

    5. Crear `tests/unit/test_distance_ors.py`:
       ```python
       """Tests unitarios para el cliente ORS de get_distance_matrix."""
       from unittest.mock import patch, Mock
       import pytest
       from apps.optimizer.services.distance import get_distance_matrix

       ORS_RESPONSE = {
           "distances": [[0.0, 5.2], [5.2, 0.0]],
           "durations": [[0.0, 1200.5], [1200.5, 0.0]],
       }

       def test_ors_matrix_success():
           """ORS response se parsea correctamente: km y minutos."""
           mock_resp = Mock()
           mock_resp.json.return_value = ORS_RESPONSE
           mock_resp.raise_for_status.return_value = None
           points = [(-5.99, 37.38), (-6.00, 37.39)]
           with patch("apps.optimizer.services.distance.requests.post", return_value=mock_resp):
               with patch("apps.optimizer.services.distance.settings") as mock_settings:
                   mock_settings.ORS_API_KEY = "test-key"
                   dist_km, time_min = get_distance_matrix(points)
           assert dist_km[0][1] == pytest.approx(5.2)
           assert time_min[0][1] == pytest.approx(1200.5 / 60)

       def test_ors_fallback_on_connection_error():
           """ConnectionError al llamar ORS activa fallback haversine."""
           import requests
           points = [(-5.99, 37.38), (-6.00, 37.39)]
           with patch("apps.optimizer.services.distance.requests.post", side_effect=requests.ConnectionError):
               with patch("apps.optimizer.services.distance.settings") as mock_settings:
                   mock_settings.ORS_API_KEY = "test-key"
                   dist_km, time_min = get_distance_matrix(points)
           # Fallback haversine devuelve valores numéricos
           assert len(dist_km) == 2
           assert len(dist_km[0]) == 2

       def test_empty_ors_key_uses_fallback():
           """Sin ORS_API_KEY se usa fallback haversine directamente."""
           points = [(-5.99, 37.38), (-6.00, 37.39)]
           with patch("apps.optimizer.services.distance.settings") as mock_settings:
               mock_settings.ORS_API_KEY = ""
               dist_km, time_min = get_distance_matrix(points)
           assert len(dist_km) == 2
       ```
  </action>
  <verify>
    <automated>cd C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg && docker compose -f docker-compose.dev.yml exec backend pytest tests/unit/test_distance_ors.py -v --tb=short</automated>
  </verify>
  <done>3 tests pasan en verde. distance.py llama a api.openrouteservice.org. ORS_API_KEY en base.py y .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Fix settings de producción para Render + crear render.yaml</name>
  <files>
    config/settings/prod.py,
    config/settings/base.py,
    render.yaml
  </files>
  <action>
    1. Leer `config/settings/prod.py` y `config/settings/base.py` completos para entender estado actual.

    2. En `config/settings/prod.py`:
       - Añadir justo después de `SECURE_SSL_REDIRECT = True`:
         ```python
         # Render termina SSL en el load balancer y reenvía HTTP internamente.
         # SECURE_PROXY_SSL_HEADER evita redirect loops al indicar que X-Forwarded-Proto
         # es la fuente de verdad para el esquema HTTPS.
         SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")
         ```
       - NO eliminar `SECURE_SSL_REDIRECT = True` — es necesario para forzar HTTPS.

    3. En `config/settings/base.py`, localizar la sección `DATABASES`. Si usa `dj_database_url.parse()`:
       - Añadir el parámetro `engine` o verificar que maneja el prefijo:
         ```python
         import dj_database_url
         _db_url = os.environ.get("DATABASE_URL", "")
         if _db_url.startswith("postgresql://"):
             _db_url = _db_url.replace("postgresql://", "postgis://", 1)
         DATABASES = {
             "default": dj_database_url.parse(_db_url) if _db_url else {
                 "ENGINE": "django.contrib.gis.db.backends.postgis",
                 # ... resto de config dev
             }
         }
         ```
       - Si ya existe la conversión, no duplicar — verificar y documentar en comentario.

    4. Crear `render.yaml` en la raíz del repositorio con el siguiente contenido (adaptar `repo` al real si se conoce):
       ```yaml
       # render.yaml — Infraestructura de staging BarGAIN en Render.com
       # Docs: https://render.com/docs/infrastructure-as-code

       databases:
         - name: bargain-postgres
           databaseName: bargain
           user: bargain
           plan: free

       services:
         # Web Service: Django API con Gunicorn
         - type: web
           name: bargain-api
           runtime: docker
           dockerfilePath: ./backend/Dockerfile
           dockerContext: ./backend
           plan: free
           buildCommand: "python manage.py collectstatic --noinput && python manage.py migrate --noinput"
           startCommand: "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 60"
           envVars:
             - key: DJANGO_SETTINGS_MODULE
               value: config.settings.prod
             - key: SECRET_KEY
               sync: false
             - key: ALLOWED_HOSTS
               sync: false
             - key: DATABASE_URL
               fromDatabase:
                 name: bargain-postgres
                 property: connectionString
             - key: REDIS_URL
               fromService:
                 name: bargain-redis
                 type: redis
                 property: connectionString
             - key: ORS_API_KEY
               sync: false
             - key: GOOGLE_MAPS_API_KEY
               sync: false
             - key: GEMINI_API_KEY
               sync: false
             - key: GOOGLE_CLOUD_VISION_API_KEY
               sync: false
             - key: CORS_ALLOWED_ORIGINS
               sync: false
             - key: SENTRY_DSN
               sync: false
           healthCheckPath: /api/v1/health/

         # Redis: broker Celery + cache Google Places
         - type: redis
           name: bargain-redis
           plan: free
           maxmemoryPolicy: noeviction

         # Background Worker: Celery worker
         - type: worker
           name: bargain-celery-worker
           runtime: docker
           dockerfilePath: ./backend/Dockerfile
           dockerContext: ./backend
           plan: free
           startCommand: "celery -A config worker -l info --concurrency 4"
           envVars:
             - key: DJANGO_SETTINGS_MODULE
               value: config.settings.prod
             - key: SECRET_KEY
               sync: false
             - key: DATABASE_URL
               fromDatabase:
                 name: bargain-postgres
                 property: connectionString
             - key: REDIS_URL
               fromService:
                 name: bargain-redis
                 type: redis
                 property: connectionString
             - key: ORS_API_KEY
               sync: false

         # Background Worker: Celery beat (scraping programado)
         - type: worker
           name: bargain-celery-beat
           runtime: docker
           dockerfilePath: ./backend/Dockerfile
           dockerContext: ./backend
           plan: free
           startCommand: "celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler"
           envVars:
             - key: DJANGO_SETTINGS_MODULE
               value: config.settings.prod
             - key: SECRET_KEY
               sync: false
             - key: DATABASE_URL
               fromDatabase:
                 name: bargain-postgres
                 property: connectionString
             - key: REDIS_URL
               fromService:
                 name: bargain-redis
                 type: redis
                 property: connectionString
       ```

    5. Actualizar `make deploy-staging` en el Makefile para que documente el flujo real:
       ```makefile
       deploy-staging:
           @echo "Deploy a Render — proceso:"
           @echo "  1. Push a main activa auto-deploy via render.yaml"
           @echo "  2. Configurar env vars en Render Dashboard (sync: false)"
           @echo "  3. Verificar health: https://<render-url>/api/v1/health/"
       ```
  </action>
  <verify>
    <automated>grep -n "SECURE_PROXY_SSL_HEADER" C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/config/settings/prod.py && grep -n "postgis" C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/config/settings/base.py && test -f C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/render.yaml && echo "OK"</automated>
  </verify>
  <done>
    - prod.py tiene SECURE_PROXY_SSL_HEADER.
    - base.py convierte postgresql:// a postgis:// en DATABASE_URL.
    - render.yaml existe en la raíz con los 5 servicios: web, redis, 2 workers.
    - Makefile actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → ORS API | API key en Authorization header — nunca en URL ni logs |
| Render env vars → Django settings | Variables `sync: false` no se muestran en render.yaml — se configuran en el dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | ORS_API_KEY en logs | mitigate | Nunca loggear el header Authorization; usar `settings.ORS_API_KEY` solo en la llamada HTTP |
| T-07-01-02 | Spoofing | DATABASE_URL inyectada en render.yaml | accept | render.yaml usa `fromDatabase` que Render gestiona internamente; no se expone en el repo |
| T-07-01-03 | Denial of Service | ORS rate limit 40 req/min | accept | Free tier suficiente para staging/demo; fallback haversine si se agota |
</threat_model>

<verification>
```bash
# 1. Tests unitarios ORS
docker compose -f docker-compose.dev.yml exec backend pytest tests/unit/test_distance_ors.py -v

# 2. Test de regresión del optimizador (no debe romperse)
docker compose -f docker-compose.dev.yml exec backend pytest tests/integration/test_optimizer_api.py -v

# 3. Verificar que el nuevo settings existe
grep "ORS_API_KEY" config/settings/base.py
grep "SECURE_PROXY_SSL_HEADER" config/settings/prod.py

# 4. Validar render.yaml tiene 5 servicios
grep "type:" render.yaml | wc -l  # debe ser >= 4 (web + redis + 2 workers)
```
</verification>

<success_criteria>
- [ ] `tests/unit/test_distance_ors.py` tiene 3 tests, todos en verde
- [ ] `test_optimizer_api.py` sigue en verde (no regresión)
- [ ] `distance.py` no contiene referencias a "graphhopper" en la lógica activa
- [ ] `ORS_API_KEY` presente en `config/settings/base.py` y `.env.example`
- [ ] `SECURE_PROXY_SSL_HEADER` presente en `config/settings/prod.py`
- [ ] `render.yaml` en la raíz con servicios: `bargain-api`, `bargain-redis`, `bargain-celery-worker`, `bargain-celery-beat`
- [ ] `render.yaml` define `databases:` con `bargain-postgres`
</success_criteria>

<output>
Tras completar este plan, crear `.planning/phases/07-testing-deploy-memoria/07-01-SUMMARY.md` con:
- Cambios realizados en distance.py (antes/después clave)
- Fix de SECURE_PROXY_SSL_HEADER aplicado
- Decisión sobre DATABASE_URL conversion (verificado/añadido)
- Estado de tests: N/N verde
- Notas sobre render.yaml para el siguiente agente
</output>
