---
phase: 07-testing-deploy-memoria
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01  # render.yaml y ORS deben existir antes del deploy real
files_modified:
  - .github/workflows/ios-build.yml
  - frontend/ExportOptions.plist
  - Makefile
autonomous: true
requirements:
  - NFR-04
user_setup:
  - service: render
    why: "Deploy del backend de staging — ejecutar después de 07-01"
    steps:
      - "1. Conectar el repo GitHub a Render: render.com → New → Blueprint → seleccionar bargain-tfg"
      - "2. Render crea los 5 servicios desde render.yaml automáticamente"
      - "3. Configurar las env vars marcadas con sync:false en el Dashboard de Render:"
      - "   SECRET_KEY, ALLOWED_HOSTS (el hostname de Render), ORS_API_KEY,"
      - "   GOOGLE_MAPS_API_KEY, GEMINI_API_KEY, GOOGLE_CLOUD_VISION_API_KEY, CORS_ALLOWED_ORIGINS"
      - "4. Ejecutar migraciones tras el primer deploy: Render → Shell → python manage.py migrate"
      - "5. Crear superusuario: python manage.py createsuperuser"
      - "6. Anotar la URL pública (ej: https://bargain-api.onrender.com) para los tests Playwright"
  - service: github-actions
    why: "Build iOS requiere runner macos-latest (no disponible localmente en Windows)"
    steps:
      - "No se requiere configuración adicional en GitHub Actions si el repo es público"
      - "Para EXPO_PUBLIC_GOOGLE_MAPS_KEY: GitHub repo → Settings → Secrets → Actions → New secret"

must_haves:
  truths:
    - "ios-build.yml existe y se puede disparar manualmente con workflow_dispatch"
    - "ExportOptions.plist existe en frontend/ para la exportación unsigned"
    - "El workflow genera un artefacto BarGAIN.ipa descargable"
  artifacts:
    - path: ".github/workflows/ios-build.yml"
      provides: "Workflow iOS build con macos-latest"
      contains: "workflow_dispatch"
    - path: "frontend/ExportOptions.plist"
      provides: "Configuración de exportación unsigned para Sideloadly"
      contains: "ad-hoc"
---

<objective>
Crear el workflow de GitHub Actions que compila la app iOS con `macos-latest` y genera un `.ipa`
unsigned descargable como artefacto. También documentar el proceso de deploy staging en Render
y el procedimiento de sideload con Sideloadly para la demo.

Purpose: La demo de la defensa requiere la app corriendo en iPhone real. iOS no se puede compilar
en Windows — GitHub Actions `macos-latest` es la única opción sin Mac local (D-05).
Output: .github/workflows/ios-build.yml ejecutable, ExportOptions.plist, e instrucciones de sideload.
</objective>

<execution_context>
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-testing-deploy-memoria/07-CONTEXT.md
@.planning/phases/07-testing-deploy-memoria/07-RESEARCH.md

<interfaces>
<!-- frontend/app.json (verificado en RESEARCH.md): -->
<!-- "name": "BarGAIN" -->
<!-- "ios": { "supportsTablet": true, "bundleIdentifier": "com.bargain.app" } -->
<!-- No existe frontend/ios/ — expo prebuild lo generará -->

<!-- CI existente: .github/workflows/ci-backend.yml -->
<!-- Usa: actions/checkout@v4, actions/cache@v4 -->
<!-- Triggereado en push a main/develop con path filter -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ExportOptions.plist para IPA unsigned</name>
  <files>frontend/ExportOptions.plist</files>
  <action>
    Crear `frontend/ExportOptions.plist` — necesario para que `xcodebuild -exportArchive` genere
    un `.ipa` que pueda ser re-firmado por Sideloadly con un Apple ID gratuito:

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
      <key>method</key>
      <string>ad-hoc</string>
      <key>compileBitcode</key>
      <false/>
      <key>signingStyle</key>
      <string>manual</string>
      <key>signingCertificate</key>
      <string>-</string>
      <key>stripSwiftSymbols</key>
      <true/>
      <key>thinning</key>
      <string>&lt;none&gt;</string>
    </dict>
    </plist>
    ```

    Nota: `signingCertificate: "-"` indica firma vacía (unsigned). Sideloadly en Windows
    re-firma el `.ipa` con el Apple ID del usuario durante la instalación.
  </action>
  <verify>
    <automated>test -f C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/frontend/ExportOptions.plist && echo "OK"</automated>
  </verify>
  <done>ExportOptions.plist existe en frontend/ con método ad-hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Crear .github/workflows/ios-build.yml</name>
  <files>.github/workflows/ios-build.yml</files>
  <action>
    Leer `.github/workflows/ci-backend.yml` para seguir los patrones del proyecto.
    Luego crear `.github/workflows/ios-build.yml`:

    ```yaml
    name: iOS Build (unsigned IPA)
    # Disparo manual o por tag ios-* — NO en cada push (build tarda ~15 min)
    on:
      workflow_dispatch:
        inputs:
          node_version:
            description: 'Node.js version'
            required: false
            default: '22'
      push:
        tags:
          - 'ios-*'

    jobs:
      build-ios:
        name: Build unsigned iOS IPA
        runs-on: macos-latest
        timeout-minutes: 60

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: ${{ github.event.inputs.node_version || '22' }}
              cache: 'npm'
              cache-dependency-path: frontend/package-lock.json

          - name: Install frontend dependencies
            working-directory: frontend
            run: npm ci

          - name: Cache Expo CLI
            uses: actions/cache@v4
            with:
              path: ~/.npm
              key: ${{ runner.os }}-expo-cli-${{ hashFiles('frontend/package.json') }}

          - name: Expo prebuild (genera directorio ios/)
            working-directory: frontend
            run: npx expo prebuild --platform ios --clean --non-interactive
            env:
              EXPO_PUBLIC_GOOGLE_MAPS_KEY: ${{ secrets.EXPO_PUBLIC_GOOGLE_MAPS_KEY }}

          - name: Verify generated workspace
            working-directory: frontend/ios
            run: |
              echo "=== Workspaces generados ==="
              ls *.xcworkspace || echo "WARN: No xcworkspace found"
              echo "=== Schemes disponibles ==="
              xcodebuild -list 2>/dev/null | head -20 || true

          - name: Cache CocoaPods
            uses: actions/cache@v4
            with:
              path: frontend/ios/Pods
              key: ${{ runner.os }}-pods-${{ hashFiles('frontend/ios/Podfile.lock') }}
              restore-keys: |
                ${{ runner.os }}-pods-

          - name: Install CocoaPods
            working-directory: frontend/ios
            run: pod install --repo-update

          - name: Build unsigned .xcarchive
            working-directory: frontend/ios
            run: |
              WORKSPACE=$(ls *.xcworkspace | head -1)
              SCHEME=$(xcodebuild -list -workspace "$WORKSPACE" 2>/dev/null | \
                awk '/Schemes:/,0' | grep -v 'Schemes:' | grep -v '^$' | head -1 | xargs)
              echo "Building workspace: $WORKSPACE, scheme: $SCHEME"
              xcodebuild \
                -workspace "$WORKSPACE" \
                -scheme "$SCHEME" \
                -configuration Release \
                -sdk iphoneos \
                -archivePath ../../build/BarGAIN.xcarchive \
                CODE_SIGNING_ALLOWED=NO \
                CODE_SIGNING_REQUIRED=NO \
                archive | xcpretty --color || \
              xcodebuild \
                -workspace "$WORKSPACE" \
                -scheme "$SCHEME" \
                -configuration Release \
                -sdk iphoneos \
                -archivePath ../../build/BarGAIN.xcarchive \
                CODE_SIGNING_ALLOWED=NO \
                CODE_SIGNING_REQUIRED=NO \
                archive

          - name: Export unsigned .ipa
            run: |
              xcodebuild \
                -exportArchive \
                -archivePath build/BarGAIN.xcarchive \
                -exportPath build/output \
                -exportOptionsPlist frontend/ExportOptions.plist \
                -allowProvisioningUpdates

          - name: List build output
            run: |
              echo "=== Contenido de build/output ==="
              ls -la build/output/ || echo "No output directory"
              find build/ -name "*.ipa" 2>/dev/null

          - name: Upload IPA artifact
            uses: actions/upload-artifact@v4
            with:
              name: BarGAIN-unsigned-${{ github.sha }}
              path: |
                build/output/*.ipa
                build/output/**/*.ipa
              retention-days: 7
              if-no-files-found: warn
    ```

    NOTA DE IMPLEMENTACIÓN: El workflow descubre dinámicamente el workspace y el scheme
    tras `expo prebuild` usando `ls *.xcworkspace` y `xcodebuild -list`, evitando asumir
    el nombre exacto. El fallback a `xcodebuild` sin `xcpretty` es necesario si `xcpretty`
    no está instalado en el runner.
  </action>
  <verify>
    <automated>
      # Verificar sintaxis YAML del workflow
      python -c "import yaml; yaml.safe_load(open('.github/workflows/ios-build.yml'))" && echo "YAML OK"
    </automated>
  </verify>
  <done>
    - ios-build.yml existe en .github/workflows/
    - YAML es válido (no errores de sintaxis)
    - Workflow configurable con workflow_dispatch
  </done>
</task>

<task type="manual">
  <name>Task 3: Documentar proceso de Render staging deploy</name>
  <files>docs/decisiones/011-deploy-staging.md</files>
  <action>
    Leer `docs/decisiones/011-deploy-staging.md` para ver el estado actual del ADR.
    Añadir al final del ADR-011 una sección de "Instrucciones de despliegue" con el
    procedimiento paso a paso para el deploy en Render.

    Añadir esta sección al final del archivo:

    ```markdown
    ## Instrucciones de despliegue en Render (actualizado 2026-04-08)

    ### Prerequisitos
    - Plan 07-01 completado (`render.yaml` en raíz del repo, ORS integrado)
    - Cuenta en Render.com conectada al repositorio GitHub

    ### Proceso de despliegue inicial

    1. **Crear Blueprint en Render:**
       - Render Dashboard → New → Blueprint
       - Seleccionar repositorio `bargain-tfg`
       - Render detecta `render.yaml` y crea los 5 servicios automáticamente:
         `bargain-api`, `bargain-postgres`, `bargain-redis`, `bargain-celery-worker`, `bargain-celery-beat`

    2. **Configurar variables de entorno (sync: false):**
       En el Dashboard de Render para el servicio `bargain-api`:
       ```
       SECRET_KEY=<generar con: python -c "import secrets; print(secrets.token_urlsafe(50))">
       ALLOWED_HOSTS=bargain-api.onrender.com
       ORS_API_KEY=<clave de openrouteservice.org>
       GOOGLE_MAPS_API_KEY=<Google Cloud Console>
       GEMINI_API_KEY=<Google AI Studio>
       GOOGLE_CLOUD_VISION_API_KEY=<Google Cloud Console>
       CORS_ALLOWED_ORIGINS=http://localhost:5173,https://bargain-api.onrender.com
       ```
       Repetir `DATABASE_URL`, `REDIS_URL`, `SECRET_KEY`, `DJANGO_SETTINGS_MODULE` para los workers.

    3. **Primer deploy y migraciones:**
       ```bash
       # Tras el primer deploy exitoso, ejecutar desde Render Dashboard → Shell:
       python manage.py migrate --noinput
       python manage.py createsuperuser
       ```

    4. **Verificar salud del servicio:**
       ```
       curl https://bargain-api.onrender.com/api/v1/health/
       # Esperado: {"status": "ok"}
       ```

    5. **Re-despliegues automáticos:**
       Cada push a `main` dispara un nuevo deploy (configurado en render.yaml).

    ### Sideload iOS para la demo

    1. Ir a `.github/workflows/ios-build.yml` → Actions → Run workflow
    2. Descargar el artefacto `BarGAIN-unsigned-*.ipa` del run
    3. En Windows: abrir Sideloadly → arrastar el `.ipa` → conectar iPhone por USB
    4. Iniciar sesión con Apple ID gratuito → Install
    5. En iPhone: Settings → General → VPN & Device Management → confiar en el certificado
    6. **Programar re-sideload 1-2 días antes de la defensa** (certificado gratuito caduca en 7 días)
    ```
  </action>
  <verify>
    <automated>grep -c "Instrucciones de despliegue" docs/decisiones/011-deploy-staging.md</automated>
  </verify>
  <done>ADR-011 tiene sección "Instrucciones de despliegue" con todos los pasos documentados.</done>
</task>

<task type="auto">
  <name>Task 4: Actualizar Makefile con targets de deploy y iOS</name>
  <files>Makefile</files>
  <action>
    Leer el Makefile actual y actualizar el target `deploy-staging` ya existente,
    y añadir un nuevo target `ios-build`.

    Reemplazar el target `deploy-staging` con:
    ```makefile
    deploy-staging: ## Desplegar a Render staging (requiere render.yaml y env vars configuradas)
    	@echo "Deploy a Render staging:"
    	@echo "  1. Asegúrate de que render.yaml está en la raíz y los cambios están en main"
    	@echo "  2. git push origin main  (dispara auto-deploy en Render)"
    	@echo "  3. O ve a Render Dashboard → bargain-api → Manual Deploy"
    	@echo ""
    	@echo "  Variables requeridas en Render (sync:false):"
    	@echo "  SECRET_KEY, ALLOWED_HOSTS, ORS_API_KEY, GOOGLE_MAPS_API_KEY,"
    	@echo "  GEMINI_API_KEY, GOOGLE_CLOUD_VISION_API_KEY, CORS_ALLOWED_ORIGINS"
    	@echo ""
    	@echo "  Verificar: curl https://<render-url>/api/v1/health/"
    ```

    Añadir nuevo target:
    ```makefile
    ios-build: ## Disparar build iOS en GitHub Actions (requiere push al repo)
    	@echo "Build iOS en GitHub Actions:"
    	@echo "  Opción 1: GitHub → Actions → 'iOS Build' → Run workflow"
    	@echo "  Opción 2: git tag ios-$(shell date +%Y%m%d-%H%M%S) && git push --tags"
    	@echo ""
    	@echo "  Artefacto: BarGAIN-unsigned-*.ipa (disponible 7 días)"
    	@echo "  Sideload: Sideloadly en Windows con Apple ID gratuito"
    	@echo "  IMPORTANTE: Re-sideload 1-2 días antes de la defensa (cert caduca en 7 días)"
    ```
  </action>
  <verify>
    <automated>grep -n "ios-build" C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/Makefile | head -5</automated>
  </verify>
  <done>Makefile tiene target `ios-build` con instrucciones de sideload y re-sideload timing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Apple ecosystem | CODE_SIGNING_ALLOWED=NO — no se usan certificados de Apple en CI |
| Sideloadly → iPhone | Re-firma con Apple ID del usuario — certificado gratuito caduca en 7 días |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | EXPO_PUBLIC_GOOGLE_MAPS_KEY en Actions | mitigate | Secret en GitHub Secrets — no en código fuente |
| T-07-03-02 | Denial of Service | Cert caducado el día de la defensa | mitigate | Re-sideload planificado 1-2 días antes; anotado en Makefile y ADR-011 |
</threat_model>

<verification>
```bash
# 1. Validar YAML del workflow
python -c "import yaml; yaml.safe_load(open('.github/workflows/ios-build.yml'))" && echo "OK"

# 2. ExportOptions.plist existe y tiene método ad-hoc
grep "ad-hoc" frontend/ExportOptions.plist

# 3. ADR-011 actualizado con instrucciones de deploy
grep "Instrucciones de despliegue" docs/decisiones/011-deploy-staging.md

# 4. Makefile tiene ios-build
grep "ios-build" Makefile

# 5. El workflow se puede disparar manualmente (verificación visual en GitHub Actions)
# GitHub → Actions → iOS Build (unsigned IPA) → Run workflow
```
</verification>

<success_criteria>
- [ ] `.github/workflows/ios-build.yml` existe con `workflow_dispatch` y `push tags: 'ios-*'`
- [ ] Workflow usa `macos-latest`, `expo prebuild --platform ios --clean`, y `xcodebuild`
- [ ] Workflow descubre workspace/scheme dinámicamente (no hardcoded)
- [ ] `frontend/ExportOptions.plist` existe con `method: ad-hoc` y sin firma
- [ ] ADR-011 tiene sección con instrucciones de deploy Render + sideload procedure
- [ ] Makefile tiene target `ios-build` con nota de re-sideload 1-2 días antes
- [ ] YAML del workflow es sintácticamente válido
- [ ] Commit: `chore(ci): añadir workflow iOS build y proceso deploy Render (F7-03)`
</success_criteria>

<output>
Tras completar este plan, crear `.planning/phases/07-testing-deploy-memoria/07-03-SUMMARY.md` con:
- Estado del workflow (disparado y resultado, o pendiente de disparar)
- URL del servicio Render si el deploy se realizó
- Nota sobre re-sideload timing para el agente de cierre
</output>
