---
phase: 07-testing-deploy-memoria
plan: 04
type: execute
wave: 2
depends_on:
  - 07-02  # Los resultados E2E deben estar listos antes de documentar Cap. 10
files_modified:
  - docs/memoria/08-diseno-implementacion.md
  - docs/memoria/09-manual-usuario.md
  - docs/memoria/10-pruebas.md
  - docs/memoria/11-conclusiones.md
  - memoriaTFG/Plantilla TfG/proyect.tex
  - memoriaTFG/Plantilla TfG/Capitulos/cap08.tex
  - memoriaTFG/Plantilla TfG/Capitulos/cap09.tex
  - memoriaTFG/Plantilla TfG/Capitulos/cap10.tex
  - memoriaTFG/Plantilla TfG/Capitulos/cap11.tex
autonomous: false  # Requiere capturas reales del sistema desplegado para Cap. 9
requirements:
  - NFR-02
  - NFR-04
  - NFR-05
user_setup:
  - service: none
    why: "Capítulos 8, 10, 11 se pueden redactar con la información disponible"
    steps:
      - "Cap. 9 (Manual de usuario) requiere capturas de pantalla de la app en Render y iPhone"
      - "Para las capturas: levantar backend Docker local o apuntar al staging de Render"
      - "Para iPhone: necesitas el .ipa instalado (ver Plan 07-03)"
      - "Capturas web: usar Chrome en localhost:5173 con el web companion"

must_haves:
  truths:
    - "Cap. 8 actualizado con arquitectura F5 (ORS) y F6 (portal business, notificaciones)"
    - "Cap. 10 documenta resultados E2E Playwright y validaciones NFR-02/04/05"
    - "Cap. 11 elimina todas las referencias a F5/F6 como 'pendiente'"
    - "4 capítulos LaTeX generados en memoriaTFG/Plantilla TfG/Capitulos/"
  artifacts:
    - path: "docs/memoria/08-diseno-implementacion.md"
      provides: "Diseño e implementación actualizado con F5/F6"
      contains: "ORS"
    - path: "docs/memoria/10-pruebas.md"
      provides: "Resultados E2E + validaciones NFR documentadas"
      contains: "Playwright"
    - path: "docs/memoria/11-conclusiones.md"
      provides: "Conclusiones actualizadas (sin referencias 'pendiente')"
      contains: "completado"
    - path: "memoriaTFG/Plantilla TfG/Capitulos/cap10.tex"
      provides: "Cap. 10 en LaTeX con resultados reales"
      contains: "Playwright"
---

<objective>
Actualizar los 4 capítulos de la memoria del TFG que requieren contenido real de las fases
completadas (F5/F6), y convertirlos a LaTeX usando la plantilla oficial ETSII. También actualizar
el archivo principal `proyect.tex` para incluir los nuevos capítulos.

Purpose: La memoria es el entregable principal del TFG. Los 4 capítulos con trabajo pendiente
(08, 09, 10, 11) deben reflejar el estado real del sistema antes de la defensa.
Output: 4 capítulos Markdown actualizados + 4 archivos LaTeX listos para compilar.
</objective>

<execution_context>
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xxnii/OneDrive/Documentos/TFG/bargain-tfg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-testing-deploy-memoria/07-CONTEXT.md
@.planning/phases/07-testing-deploy-memoria/07-RESEARCH.md

<interfaces>
<!-- Formato ETSII (D-15): Arial/Helvetica 11pt, interlineado 1.0 o 1.5 -->
<!-- Márgenes: 2.5 cm sup/inf, 3 cm laterales -->
<!-- Idioma: español -->
<!-- Resumen: máx 1 página, sin figuras ni citas -->
<!-- Plantilla LaTeX: memoriaTFG/Plantilla TfG/proyect.tex (entry point) -->
<!-- pclass.cls usa \usepackage{times} — cambiar a Helvetica/Arial -->
<!-- Capítulos ejemplo: memoriaTFG/Plantilla TfG/Capitulos/capitulo1.tex -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar Cap. 8 — Diseño e implementación (F5/F6)</name>
  <files>docs/memoria/08-diseno-implementacion.md</files>
  <action>
    Leer el archivo actual `docs/memoria/08-diseno-implementacion.md` completo.
    Leer también `CLAUDE.md` (sección modelos de datos y arquitectura) para verificar
    el estado actual de cada componente.

    Añadir o actualizar las siguientes secciones en el capítulo:

    **Sección: Módulo de Optimización (F5)**
    - Algoritmo score = w_precio*(ahorro) - w_distancia*(dist_extra) - w_tiempo*(tiempo_extra)
    - Integración ORS API como proveedor de matriz de distancias (sustituyó a Graphhopper en staging)
    - OR-Tools para resolver el problema de rutas (VRP con depot en ubicación del usuario)
    - Modelo `OptimizationResult` con campos: `user_location`, `max_distance_km`,
      `optimization_mode`, `total_price`, `total_distance_km`, `estimated_time_minutes`, `route_data`
    - Limitación de 3-4 paradas máximas por ruta

    **Sección: OCR y Procesamiento de Tickets (F5)**
    - Google Cloud Vision API como proveedor (ADR-007, sustituyó a Tesseract)
    - Pipeline: imagen → Vision API → texto → fuzzy matching contra catálogo → lista de productos
    - Endpoint `POST /api/v1/ocr/scan/`

    **Sección: Portal Business y PYME (F6)**
    - Modelo `BusinessProfile` con flujo de aprobación por admin
    - Modelo `Promotion` con `UNIQUE PARTIAL (product, store) WHERE is_active`
    - Servicio compartido `approve_proposal()` en `services.py`
    - Portal web (`frontend/web/`) con React + Ant Design: Dashboard, Prices, Promotions, Admin

    **Sección: Notificaciones (F6)**
    - Modelo `Notification` con soft-delete y campo `action_url`
    - Modelo `UserPushToken` con Expo push tokens
    - Integración email (SMTP) y push (Expo Push API)

    **Sección: Asistente LLM (F5)**
    - Google Gemini API `gemini-2.0-flash-lite` (ADR-008, sustituyó a Claude)
    - Guardrails de dominio (solo responde consultas de compra)
    - Rate limiting 30 req/hora por usuario

    NO eliminar secciones existentes (F1-F4). Solo añadir/actualizar F5/F6.
  </action>
  <verify>
    <automated>grep -c "ORS\|OpenRouteService\|Vision API\|Gemini\|Portal Business" docs/memoria/08-diseno-implementacion.md</automated>
  </verify>
  <done>Cap. 8 menciona ORS, Vision API, Gemini, Portal Business, y notificaciones push/email.</done>
</task>

<task type="manual">
  <name>Task 2: Actualizar Cap. 9 — Manual de usuario (capturas definitivas)</name>
  <files>docs/memoria/09-manual-usuario.md</files>
  <action>
    Leer `docs/memoria/09-manual-usuario.md` para ver el estado actual.

    Este capítulo requiere capturas de pantalla reales del sistema funcionando.
    Tomar capturas y documentar los siguientes flujos:

    **Capturas del web companion (localhost:5173 o Render staging):**
    1. `/login` — formulario de login
    2. `/register` — formulario de registro
    3. `/onboarding` — formulario de registro PYME
    4. `/dashboard` — panel de PYME aprobada (sección de precios)
    5. `/admin` — panel de aprobación de admin

    **Capturas de la app móvil (Expo Go o IPA en iPhone):**
    6. Pantalla de autenticación
    7. Lista de la compra (crear/editar items)
    8. Resultado de optimización con ruta
    9. Pantalla OCR (subir foto de ticket)
    10. Chat con el asistente LLM

    Para cada captura:
    - Guardar en `docs/diagramas/capturas/` con nombres descriptivos
      (ej: `web-login.png`, `mobile-optimizer-result.png`)
    - Referenciar en el Markdown con `![Descripción](../diagramas/capturas/nombre.png)`

    Actualizar el capítulo con:
    - Flujo de uso completo de usuario consumidor (pasos 1-N con capturas)
    - Flujo de uso de PYME (registro → aprobación → gestión de precios)
    - Flujo del asistente (preguntas de compra)

    NOTA: Si las capturas móviles no están disponibles aún (iOS build en progreso),
    documentar los flujos con Expo Web o simulador, marcando explícitamente que las
    capturas definitivas de iPhone se añadirán antes de la entrega.
  </action>
  <verify>
    <automated>grep -c "!\[" docs/memoria/09-manual-usuario.md</automated>
  </verify>
  <done>Cap. 9 tiene al menos 5 capturas referenciadas con la sintaxis Markdown ![...](...)</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar Cap. 10 — Pruebas (resultados E2E + NFR)</name>
  <files>docs/memoria/10-pruebas.md</files>
  <action>
    Leer `docs/memoria/10-pruebas.md` para ver el estado actual.

    Añadir/actualizar las siguientes secciones:

    **Sección: Tests E2E con Playwright (resultados de Plan 07-02)**
    ```markdown
    ### 10.4 Tests End-to-End con Playwright

    Se desarrollaron 4 tests E2E automatizados usando Playwright para verificar los flujos
    críticos del sistema integrado:

    | Flujo | Archivo | Tipo | Resultado |
    |-------|---------|------|-----------|
    | Autenticación completa | `e2e/auth.spec.ts` | UI + API | ✅ Pasado |
    | Lista → Optimizar → Ruta | `e2e/optimizer.spec.ts` | API-level | ✅ Pasado |
    | OCR ticket → lista | `e2e/ocr.spec.ts` | API-level | ✅ Pasado |
    | Business portal | `e2e/business.spec.ts` | UI | ✅ Pasado |

    Los flujos de optimizador y OCR se implementaron como tests API-level (usando el fixture
    `request` de Playwright) ya que estas funcionalidades son exclusivas de la app móvil y
    no tienen interfaz en el web companion. Esto es coherente con D-09 (flujos nativos con
    GPS/cámara cubiertos por UAT manual).

    La suite completa se ejecuta con: `cd frontend/web && npx playwright test`
    ```

    **Sección: Validación NFR-02 — Disponibilidad 99% en staging**
    ```markdown
    ### 10.5 Validación de Requisitos No Funcionales

    #### NFR-02: Disponibilidad ≥ 99% en staging (RNF-002)

    El backend de staging se desplegó en Render.com (plan gratuito) con la siguiente
    arquitectura: Web Service (Gunicorn 3 workers) + PostgreSQL + Redis + Celery worker +
    Celery beat. El panel de uptime de Render mostró [X]% de disponibilidad durante
    [N] días de monitorización. [AÑADIR CAPTURA del dashboard de Render aquí]

    El endpoint de salud `GET /api/v1/health/` responde consistentemente con HTTP 200.
    ```

    **Sección: Validación NFR-04 — Usabilidad WCAG 2.1 AA**
    ```markdown
    #### NFR-04: Usabilidad WCAG 2.1 AA y máximo 3 taps (RNF-004)

    Se realizó un spot-check de accesibilidad con Lighthouse Accessibility en los 3 flujos
    principales del web companion:

    | Flujo | Score Lighthouse Accessibility |
    |-------|-------------------------------|
    | Login | [X]/100 |
    | Registro PYME | [X]/100 |
    | Dashboard PYME | [X]/100 |

    [AÑADIR CAPTURAS de Lighthouse aquí]

    Los flujos principales se completan en ≤ 3 taps en la app móvil:
    - Ver precios cercanos: Home → Buscar → Resultados (3 taps)
    - Optimizar ruta: Lista → Optimizar → Ver ruta (3 taps)
    - Añadir item OCR: Lista → Cámara → Confirmar (3 taps)
    ```

    **Sección: Validación NFR-05 — Escalabilidad 10k usuarios**
    ```markdown
    #### NFR-05: Escalabilidad para 10.000 usuarios concurrentes (RNF-005)

    La arquitectura de BarGAIN soporta el crecimiento a 10.000 usuarios mediante:

    1. **Workers Celery independientes**: El scraping, OCR y optimización se ejecutan en
       workers separados, evitando que tareas pesadas bloqueen el API principal.

    2. **Redis como broker**: Desacopla las peticiones de usuario de las tareas costosas,
       permitiendo colas de trabajo elásticas.

    3. **PostGIS para geoespacial**: Las consultas de tiendas cercanas usan índices espaciales
       (GIST) en PostgreSQL, con complejidad O(log n) independiente del volumen de tiendas.

    4. **Stateless API**: El backend Django no mantiene estado de sesión (JWT stateless),
       lo que facilita el escalado horizontal añadiendo réplicas del Web Service.

    No se realizó un load test real (requeriría infraestructura de pago fuera del alcance del
    TFG). La justificación arquitectural es coherente con los patrones recomendados por
    Django, Celery y PostgreSQL para cargas similares.
    ```

    Actualizar tabla de cobertura de tests con los nuevos tests E2E (agregar fila al
    resumen existente o crear una nueva tabla de cobertura total).
  </action>
  <verify>
    <automated>grep -c "Playwright\|NFR-02\|NFR-04\|NFR-05\|WCAG" docs/memoria/10-pruebas.md</automated>
  </verify>
  <done>Cap. 10 tiene secciones para E2E Playwright, NFR-02, NFR-04 y NFR-05.</done>
</task>

<task type="auto">
  <name>Task 4: Actualizar Cap. 11 — Conclusiones (eliminar referencias 'pendiente')</name>
  <files>docs/memoria/11-conclusiones.md</files>
  <action>
    Leer `docs/memoria/11-conclusiones.md` completo.

    1. **Buscar y eliminar** todas las referencias a F5 o F6 como "pendiente", "en desarrollo",
       "trabajo futuro inmediato", "por implementar", "no implementado aún", u otras que
       impliquen que el sistema no está completo.

    2. **Actualizar la sección de logros** para incluir F5 y F6 como completadas:
       - Optimizador multicriterio con ORS API (F5)
       - OCR de tickets con Google Vision API (F5)
       - Asistente LLM con Gemini (F5)
       - Portal business para PYMEs con proceso de aprobación admin (F6)
       - Notificaciones push (Expo) y email (SMTP) (F6)
       - App móvil React Native con Expo (F5/F6)

    3. **Actualizar la sección de limitaciones** con limitaciones reales post-implementación:
       - El scraping de precios cubre solo 4 cadenas (Mercadona, Carrefour, Lidl, DIA)
       - Los precios tienen TTL de 48h/24h — pueden no ser completamente actuales
       - El plan de Render gratuito tiene cold starts (~30s) después de inactividad
       - La certificación iOS gratuita (Sideloadly) caduca cada 7 días
       - No hay load test real para NFR-05 (justificación arquitectural únicamente)

    4. **Actualizar la sección de trabajo futuro** para reflejar mejoras reales:
       - Expandir scrapers a más cadenas (Alcampo, Aldi, etc.)
       - Implementar precios en tiempo real via APIs oficiales de supermercados
       - Publicar app en App Store y Google Play
       - Añadir sistema de reseñas de productos
       - Integrar pagos para las PYMEs premium

    5. **Añadir párrafo de cierre** que resuma el estado final del proyecto:
       El sistema BarGAIN está completamente implementado y desplegado en staging, cubriendo
       los requisitos funcionales y no funcionales definidos. Los 4 flujos E2E críticos
       han sido verificados de forma automatizada con Playwright.
  </action>
  <verify>
    <automated>
      # Verificar que no queda "pendiente" en sentido de desarrollo incompleto
      grep -in "pendiente\|por implementar\|no implementad\|en desarrollo" docs/memoria/11-conclusiones.md | grep -v "trabajo futuro" | head -10
    </automated>
  </verify>
  <done>
    Cap. 11 no tiene referencias a F5/F6 como pendiente.
    Tiene logros actualizados con F5/F6 completadas.
    Tiene limitaciones y trabajo futuro realistas.
  </done>
</task>

<task type="auto">
  <name>Task 5: Convertir 4 capítulos a LaTeX y actualizar proyect.tex</name>
  <files>
    memoriaTFG/Plantilla TfG/Capitulos/cap08.tex,
    memoriaTFG/Plantilla TfG/Capitulos/cap09.tex,
    memoriaTFG/Plantilla TfG/Capitulos/cap10.tex,
    memoriaTFG/Plantilla TfG/Capitulos/cap11.tex,
    memoriaTFG/Plantilla TfG/proyect.tex
  </files>
  <action>
    Leer `memoriaTFG/Plantilla TfG/Capitulos/capitulo1.tex` como ejemplo del formato LaTeX
    esperado por `pclass.cls`. Luego leer `memoriaTFG/Plantilla TfG/proyect.tex` para entender
    cómo se incluyen los capítulos.

    Para cada capítulo (08, 09, 10, 11), crear el archivo `.tex` correspondiente convirtiendo
    el contenido Markdown a LaTeX usando la estructura del ejemplo. La conversión es manual
    (pandoc puede usarse como punto de partida pero requiere revisión). Estructura base:

    ```latex
    \chapter{Título del Capítulo}
    \label{cap:XX}

    \section{Primera sección}
    \label{sec:XX-1}

    Párrafo de texto. Las \textbf{palabras clave} se destacan en negrita.
    Los términos técnicos en \textit{cursiva}.

    \begin{itemize}
      \item Elemento de lista
      \item Otro elemento
    \end{itemize}

    % Para código:
    \begin{lstlisting}[language=Python, caption={Descripción del código}]
    def funcion():
        pass
    \end{lstlisting}

    % Para tablas:
    \begin{table}[htbp]
      \centering
      \begin{tabular}{|l|l|l|}
        \hline
        Columna 1 & Columna 2 & Columna 3 \\ \hline
        Dato & Dato & Dato \\ \hline
      \end{tabular}
      \caption{Título de la tabla}
      \label{tab:XX-1}
    \end{table}

    % Para imágenes (cuando estén disponibles):
    \begin{figure}[htbp]
      \centering
      \includegraphics[width=0.8\textwidth]{img/nombre-imagen}
      \caption{Pie de figura}
      \label{fig:XX-1}
    \end{figure}
    ```

    Para secciones donde faltan capturas (Cap. 9), incluir el espacio reservado:
    ```latex
    % TODO: Añadir captura de pantalla definitiva aquí
    % \includegraphics[width=0.8\textwidth]{img/capturas/nombre}
    ```

    Actualizar `proyect.tex` para incluir los nuevos capítulos en `\mainmatter`:
    ```latex
    \mainmatter
        \input{Capitulos/capitulo1}   % Introducción (existente)
        % ... capítulos existentes ...
        \input{Capitulos/cap08}       % Diseño e implementación (nuevo)
        \input{Capitulos/cap09}       % Manual de usuario (nuevo)
        \input{Capitulos/cap10}       % Pruebas (nuevo)
        \input{Capitulos/cap11}       % Conclusiones (nuevo)
    ```

    También actualizar en `proyect.tex`:
    - `\titulopro{BarGAIN: Sistema de optimización de compra inteligente}` (si no está)
    - `\tutor{Juan Vicente Gutiérrez Santacreu}`
    - `\departamento{Matemática Aplicada I}`
    - `\autores{Nicolás Parrilla Geniz}{{\ }}`
    - `\titulacion{Grado en Ingeniería Informática (Ingeniería del Software)}`

    Reemplazar `\usepackage{times}` por:
    ```latex
    \usepackage[scaled]{helvet}
    \renewcommand{\familydefault}{\sfdefault}
    ```
    (Esto aplica Helvetica, equivalente a Arial, cumpliendo ETSII D-15)
  </action>
  <verify>
    <automated>
      test -f "memoriaTFG/Plantilla TfG/Capitulos/cap08.tex" && \
      test -f "memoriaTFG/Plantilla TfG/Capitulos/cap09.tex" && \
      test -f "memoriaTFG/Plantilla TfG/Capitulos/cap10.tex" && \
      test -f "memoriaTFG/Plantilla TfG/Capitulos/cap11.tex" && \
      grep "cap08\|cap09\|cap10\|cap11" "memoriaTFG/Plantilla TfG/proyect.tex" && \
      echo "OK"
    </automated>
  </verify>
  <done>
    - 4 archivos .tex creados en Capitulos/
    - proyect.tex los incluye en \mainmatter
    - proyect.tex tiene datos del autor correctos y fuente Helvetica
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contenido Markdown → LaTeX | Conversión manual — revisar que no se pierda contenido técnico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | Datos inventados en resultados E2E | mitigate | Solo documentar resultados reales del Plan 07-02; dejar [PENDIENTE] si no está ejecutado |
| T-07-04-02 | Information Disclosure | Credenciales en capturas | mitigate | Verificar que ninguna captura expone tokens, API keys, ni datos personales de prueba |
</threat_model>

<verification>
```bash
# 1. Cap. 8 tiene contenido de F5/F6
grep -c "ORS\|Vision API\|Gemini\|Portal Business" docs/memoria/08-diseno-implementacion.md

# 2. Cap. 10 tiene secciones NFR
grep -c "NFR-02\|NFR-04\|NFR-05\|Playwright" docs/memoria/10-pruebas.md

# 3. Cap. 11 sin referencias de trabajo incompleto en F5/F6
grep -in "pendiente" docs/memoria/11-conclusiones.md | grep -v "trabajo futuro"

# 4. Archivos LaTeX creados
ls "memoriaTFG/Plantilla TfG/Capitulos/cap0"*.tex

# 5. proyect.tex incluye los nuevos capítulos
grep "cap08\|cap09\|cap10\|cap11" "memoriaTFG/Plantilla TfG/proyect.tex"
```
</verification>

<success_criteria>
- [ ] `docs/memoria/08-diseno-implementacion.md` menciona ORS, Vision API, Gemini, Portal Business
- [ ] `docs/memoria/09-manual-usuario.md` tiene ≥ 5 capturas referenciadas
- [ ] `docs/memoria/10-pruebas.md` tiene tabla E2E Playwright y secciones NFR-02/04/05
- [ ] `docs/memoria/11-conclusiones.md` no contiene F5/F6 como "pendiente" (fuera de trabajo futuro)
- [ ] `memoriaTFG/Plantilla TfG/Capitulos/cap08.tex` a `cap11.tex` creados
- [ ] `memoriaTFG/Plantilla TfG/proyect.tex` incluye los 4 capítulos nuevos
- [ ] `proyect.tex` usa Helvetica (no Times) y tiene datos del autor correctos
- [ ] Commit: `docs(memoria): actualizar caps 8-11 y añadir LaTeX (F7-04)`
</success_criteria>

<output>
Tras completar este plan, crear `.planning/phases/07-testing-deploy-memoria/07-04-SUMMARY.md` con:
- Estado de cada capítulo (actualizado / con TODOs pendientes de capturas)
- Nota sobre capturas pendientes para Cap. 9 (si no están todas)
- Instrucción para compilar el PDF: `cd "memoriaTFG/Plantilla TfG" && pdflatex proyect.tex && bibtex proyect && pdflatex proyect.tex && pdflatex proyect.tex`
</output>
